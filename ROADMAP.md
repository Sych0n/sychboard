# SychBoard — Roadmap & Audit

> Audited 2026-07-18 (Claude session). Branch `game-rebuild` = the real, current version:
> SQLite (src/db.js), full gamification (quests/XP/levels/ranks/badges/streaks/SychCoins/shop),
> Three.js boot orb, GSAP. `main` is the OLD localStorage version — **do not develop on main**.
> ⚠️ CI releases + auto-updates the installed app on every push to `main`. Merging game-rebuild
> into main is the "ship it" moment — deliberate, only after the blockers below are fixed.

## 🚨 CRITICAL — live API key exposure via Netlify PWA (2026-08-01, needs Daniel's decision)

`netlify/edge-functions/inject-env.js` reads `GROQ_API_KEY`/`YOUTUBE_API_KEY`/`YOUTUBE_CHANNEL_ID`/
`TRADING212_API_KEY` from Netlify's server-side env and injects them **verbatim into every HTML
response** as `window.__ENV__=...`, with no auth check anywhere in `netlify.toml` (no Netlify
Identity, no basic-auth header, no `_headers` access rule) — anyone with the site URL gets all
four real secrets via View Source, no login required. This is on **`main`** (confirmed via
`git show main:netlify/edge-functions/inject-env.js`), introduced by commit `7f8dbf5` on
2026-04-21 — i.e. it has been live in whatever is actually deployed to Netlify for ~3.5 months,
independent of the `game-rebuild`/`main` game-system work this roadmap otherwise tracks. This
routine only audits `game-rebuild`'s Electron app (db.js/main.js/renderer.js/preload.js/mcp) and
has never looked at the `netlify/` PWA surface before now; this was found by finally checking it.

**Not fixed by this routine** — deliberately left alone, flagged instead, because:
- It's live infrastructure (real Netlify env secrets), not a local code change to verify by reading.
- The right fix depends on facts only Daniel has: is the Netlify site actually public or
  already access-restricted at the Netlify dashboard level (password protection / private beta
  isn't visible from the repo)? Are these keys still active, or already rotated/revoked?
- Even the correct code fix is an architecture call, not a one-liner: either move the
  Groq/YouTube/Trading212 calls behind a Netlify function that keeps the keys server-side (the
  edge function would proxy requests instead of exposing keys to the client), or gate the whole
  site behind auth — either is real design work, not a "one focused item" bug fix.
- If the keys have been exposed for 3.5 months, the immediate action (rotate them) has to happen
  regardless of which code fix is chosen, and that's Daniel's call to make right away.

**Recommended immediate action:** rotate all four keys (Groq, YouTube Data API, Trading 212) and
check the Netlify dashboard for whether the site is public. Then decide the code fix (server-side
proxy vs. removing the PWA's dependency on live keys vs. access-gating the site).

## Audit verdict

db.js is genuinely well-engineered: versioned migrations, transactions everywhere,
soft-reset streaks with freeze tokens, coin clawback on quest-untick (no toggle farming),
category sweep bonuses, retroactive coin grants. The IPC surface is complete and clean.

## 🐛 Bugs found — ✅ ALL 4 FIXED 2026-07-18 (verified against real SQLite in isolated tests)

1. **Streak rollover bug** — `completeQuest` computes "yesterday" from the real clock, but
   `getAppDate()` shifts the app-day before the 4am rollover. Completing a quest between
   midnight and 4am after a valid previous-day completion can wrongly soft-reset the streak.
   Fix: derive yesterday from `getAppDate()` minus 1, not `new Date()` minus 1.
2. **Weekly quests are farmable daily** — `quest_completions` is unique per (quest, app_date)
   and `listQuests` shows weekly quests every day, so a 50xp weekly can be completed 7×/week.
   Fix: for `frequency='weekly'`, check completion within the ISO week, not the day.
3. **Sweep bonus invisible** — the +10xp category sweep updates the DB after `newTotalXp` is
   computed, so the returned total (and the UI) lags until next refresh.
4. `getStreaks`/`getXpHistory` mix real dates with app-dates (same rollover blind spot as #1).

## 🚀 v1.1 (game release) — path to merging into main

1. ~~Fix the four bugs above.~~ ✅ done.
2. **Data migration** — see spec below (2026-07-19). Verdict: no code needed for v1.1;
   revisit "legacy seed" as an optional v1.2+ nice-to-have.
3. ~~Shop completion~~ ✅ verified already done (2026-07-19) — see note below.
4. ~~Boot orb polish (Three.js) — user-flagged.~~ ✅ done (2026-07-19) — added window-resize
   handling for the boot screen (star field canvas + Three.js orb renderer), which previously
   sized once at boot start and never adapted if the Electron window was resized before launch.
5. Then: bump version, merge → main, CI ships it, installed app auto-updates.

### Shop — verified complete (2026-07-19)

Checked `src/renderer.js` (`SHOP_ITEMS`, `rShop`, `buyShopItem`, `equipShopItem`, `loadEquips`,
`applyEquips`), `src/styles.css` (`data-bg`/`data-card`/`data-orbprev` rules), `src/db.js`
(`purchaseItem`, `shop_owned` setting), `main.js` (`shop:purchase`, `coins:*`,
`streaks:add-freeze`, `settings:*` IPC handlers) and `preload.js` (bridge). Full catalog exists
(6 accents, 3 fonts, 4 backgrounds, 3 card styles, 3 boot-orb themes, 1 consumable). Equip state
persists via `settings` table (`equip_accent/font/bg/card/orb`), and `loadEquips()` is awaited
*before* `startBoot()` on normal app init (`src/renderer.js:2399-2405`), so the boot orb and
theme render with the right equipped items from the first frame. No gaps found — nothing to do
here for v1.1.

### Data migration — spec (2026-07-19)

**The original concern was partly wrong.** Re-checked what actually happens when game-rebuild
merges into main:

- `appId`/`productName` are unchanged (`com.sychboard.app` / `SychBoard` in both branches'
  `package.json`), so Electron's `userData` path — and therefore the `localStorage` partition —
  is identical across the update. Nothing gets wiped by the app switching versions.
- `src/renderer.js` `load()`/`save()` (lines ~90-129) still read/write the exact same `sb4_*`
  keys for finance, habits (`st.habits`/`st.habitHistory`), journal, sleep, goals, todos, etc.
  That data model was **not touched** by the SQLite rebuild — it's the same code path as on
  `main` today.
- `src/db.js` (quests/XP/streaks/badges/shop/SychCoins) is purely additive: it never reads or
  writes any `sb4_` key. It's a separate SQLite file (`sychboard.db` in `userData`).

So merging to main will **not** wipe finances/habits/journal/sleep — they load exactly as
before, untouched. The "shipping wipes user data" framing in the original audit was incorrect.

**The real (much smaller) gap:** the new gamification layer starts at Level 1 / 0 XP / 0
SychCoins / 0-day streaks for everyone, including users with months of history in the old
localStorage habit tracker. Not data loss — but it may *feel* like a downgrade the first time an
existing user opens the updated app.

**Two options considered:**

- **Option A — do nothing (recommended for v1.1).** Ship as-is. The new gamification system
  starts fresh; the old localStorage-backed sections (Habits, Finance, Journal, Sleep, etc.)
  keep working exactly as they do today, unaffected. Zero risk of a buggy one-time import
  corrupting `profile.total_xp`/`sychcoins` or double-counting streaks.
- **Option B — one-time "legacy seed" import (deferred, v1.2+ nice-to-have).** On first launch
  after the update, read `st.habitHistory` (`{date: {d: doneCount, t: totalCount}}`, no
  per-category or per-quest breakdown — see `recordHabitHistory()` at
  `src/renderer.js:2001`) and backfill an *approximate* XP/coin grant plus a "legacy days
  active" badge. This is inherently a heuristic: `habitHistory` has no mapping to the four
  gamification categories (health/productivity/creativity/finance) or to specific quests, so
  any XP import is an estimate, not a faithful replay of what the user "would have earned."

  Sketch if pursued later:
  1. New `settings` key `legacy_seeded` (absent = not yet run).
  2. `db.js: importLegacyHabitHistory(habitHistoryObj)` — guarded by
     `if (getSetting('legacy_seeded')) return { skipped: true }`; inside one transaction, walk
     `habitHistory` dates ascending, grant XP proportional to `d/t` per day (flat rate, e.g.
     5xp × completion%) into `xp_log` (`source_type='legacy_import'`), bump
     `profile.total_xp`; do **not** attempt to backfill per-category `streaks` rows (no
     category data exists to attribute it to) — leave those at 0 and let real usage build them
     naturally; finish with `setSetting('legacy_seeded', '1')`.
  3. New IPC handler `game:import-legacy` in `main.js`, called once from renderer's boot
     sequence after `load()`, passing `st.habitHistory`.
  4. Must be idempotent (checked via the flag) since it'd be safe to call on every boot.
  5. Needs a dry-run against a synthetic `habitHistory` fixture before ever touching a real
     profile — an off-by-one in the running-total math would corrupt `xp_log`.

**Recommendation:** ship v1.1 with Option A (no migration code). The thing the original audit
was worried about — real user data loss — doesn't happen. Option B is a nice-to-have UX polish
item, not a correctness requirement, and its heuristic nature makes it lower priority than the
rest of the v1.1/v1.2 queue.

## 📈 v1.2+ — real data & feel

> Daniel greenlit (2026-07-28, live chat) moving the autonomous routine into this list once
> bugs/v1.1/code-quality ran dry: tackle items in ease/self-containment order, work
> autonomously, only stop for clarification/approval on something genuinely ambiguous or
> risky (flagged via the routine's notification, same as always). The merge-to-main decision
> is explicitly excluded from that — never do it unprompted regardless of what else is
> greenlit. Rough ease ordering for what's left, easiest/most self-contained first:
> 1. ~~Data export/backup~~ ✅ done (2026-07-28) — see below.
> 2. ADHD loop tuning — UI-only, no external deps.
> 3. AI: bills/subscriptions into the prompt — prompt-construction change, low risk.
> 4. Installer polish — touches packaging/build config; can't visually verify without a
>    display, so keep changes conservative and lean on `node --check` + careful reading.
> 5. YouTube API / Trading 212 live data — needs real API keys/live testing this environment
>    doesn't have; lowest priority for unattended work.

- ~~Data export/backup (from old MEMORY.md backlog) — one-click JSON dump + restore.~~ ✅ done
  2026-07-28 — see "Data export/backup" below.
- ADHD loop tuning — ✅ done 2026-07-29: "next best action" nudge on Home; confetti/sound on
  quest complete; daily quest auto-reset notification at rollover (see log for all three).
- Installer polish: signed feel, first-run onboarding, tray icon, launch-on-startup toggle.
- ~~AI: feed bills/subscriptions into the prompt (proactive warnings); apply parseMD beyond
  journal.~~ ✅ done 2026-07-29 — see log below.
- YouTube API + Trading 212 live data on the dashboard cards (keys via the dev panel).

### Data export/backup — done (2026-07-28)

Extended the existing (half-working) Settings → Data Export button: it previously only backed
up `localStorage` (finance/habits/journal/etc.), silently missing all SQLite gamification state
and offering no restore path. `db.exportGameData()`/`importGameData()` (transactional,
validated, atomic on failure) plus new IPC/preload plumbing and a "Restore from Backup…" button
now cover the whole app. Verified round-trip-exact against a real SQLite DB. See the autonomous
work log entry below for full detail.

## 🔭 Ideas (proposals — Daniel decides)

- Chuck Bird crossover: SychBoard quest completions grant Chuck feathers via a tiny API.
- Weekly review screen: XP graph, streak health, badge progress, AI-written week summary.
- Mobile PWA parity for the game system (currently desktop-first).

## Autonomous work log

- 2026-07-18: repo audited; uncommitted game work rescued to `game-rebuild`; this roadmap added.
- 2026-07-18: all 4 audit bugs fixed in src/db.js (app-date streak yesterday, weekly once-per-week guard + display, sweep bonus in return, xp history rollover-aware). Tested with temp DB. Known minor quirk: a weekly completed on a previous day can't be un-ticked later in the week (uncomplete matches same-day only).
- 2026-07-19: audited shop system end-to-end (renderer/css/db/main/preload) — already fully implemented, no work needed. Wrote the localStorage-to-SQLite data migration spec: corrected the audit's premise (merging to main does not wipe localStorage data — appId/userData path is unchanged and old sb4_ keys are untouched by the SQLite rebuild), recommended shipping v1.1 without migration code, and sketched an optional v1.2+ "legacy seed" XP import as a deferred nice-to-have.
- 2026-07-19: boot orb polish — the Three.js orb and star-field canvas on the boot screen sized themselves once from `window.innerWidth` at boot start with no resize handling; added a `bootResizeHandler` wired up in `startBoot()` (star canvas dims + `renderer.setSize` on the orb) and torn down in both `startBoot()`'s own reset path and `enterApp()` to avoid leaking listeners across boots. All four v1.1 queue items are now done except the merge-to-main step itself.
- 2026-07-19: input validation pass on IPC handlers in main.js — the gamification handlers (`quests:complete/uncomplete`, `coins:award`, `shop:purchase`, `streaks:add-freeze`, `xp:history`, `settings:get/set`, `profile:update-name`) took renderer-supplied args straight into better-sqlite3 params with no type/shape checks (undefined/NaN/objects throw opaque native errors there); added `isPositiveInt`/`isFiniteNumber`/`isNonEmptyString` guards that reject bad input before it reaches db.js, plus a 40-char cap on display name. Also guarded `ytPath` in `youtube-fetch`/`youtube-analytics-fetch` against non-string input.
- 2026-07-19: error handling — main.js had no process-level safety net: an uncaught exception or unhandled promise rejection anywhere in the main process (outside the per-handler try/catch blocks already added) would silently crash the whole Electron app with nothing logged for the user to diagnose. Added `process.on('uncaughtException'/'unhandledRejection')` handlers that log and let the process keep running instead of dying. All v1.1 bugs/items/code-quality passes are now done; only the merge-to-main decision remains.
- 2026-07-20: re-audited db.js and main.js for anything the previous passes missed. Found `getXpHistory` was only half-fixed for bug #4 — its bucket *mapping* correctly shifts `xp_log.occurred_at` by the rollover hour to compute each entry's app-date, but the day-*list* it generates (the last N labeled bars) was still anchored to `new Date()` (real wall clock) instead of `getAppDate()`. Between midnight and the rollover hour this mislabeled which bar "today's" XP landed on, off by one day. Fixed by anchoring the day loop to `new Date(getAppDate())`. Reviewed all IPC handlers in main.js (t212/youtube/oauth/gamification) — validation and error handling on the rest look sound, nothing else found this pass.
- 2026-07-20: fixed a dead feature found in `completeQuest` (src/db.js) — `freeze_tokens` were granted (every 7-day streak milestone, plus the 150-coin "Streak Freeze" shop item explicitly promising "Protects a missed day") but never actually *consumed* anywhere, so a missed day always broke the streak regardless of tokens in stock; players could stockpile freezes that did nothing. Added the missing consumption path: if exactly one day was missed and a token is available, the streak now bridges the gap (spends one token, streak continues) instead of soft/hard resetting; unrelated cases (0-day gap, ≥2-day gap, or no tokens) are unchanged. Surfaced a `streak.freezeUsed` flag through the return value and a "❄️ Streak Freeze used" toast in the renderer. Verified the date-boundary logic (consecutive day, 1-day gap with/without token, 2-day gap, first-ever completion) with a standalone Node reproduction of the branch logic — `better-sqlite3` isn't installed in this environment so a full DB-backed test wasn't run; `node --check` passes on all four core files.
- 2026-07-20: fixed a streak-integrity bug in `uncompleteQuest` (src/db.js) — reverting the last completion of the day for a category unconditionally set `last_completion_date=NULL`. Since `completeQuest` treats a null `last_completion_date` as "first ever completion" (the `!lastDate` branch, which always grants +1 and never breaks/soft-resets), a single complete→uncomplete cycle made that category's streak *permanently unbreakable* afterward — any later completion just incremented it regardless of how many days had actually been skipped in between. Fixed by restoring `last_completion_date` to the true most-recent prior completion date in that category (querying `quest_completions` for `app_date < today`), falling back to NULL only when none exists. Verified with a standalone Node simulation of the streak transition branches (complete day1+day2, uncomplete day2, skip day3/4, complete day5 — confirms the streak now correctly resets to 1 instead of continuing at 2); `node --check` passes on all four core files.
- 2026-07-20: closed a leftover gap from the bug #3 fix (sweep bonus lag) — `completeQuest` (src/db.js) has returned `sweepBonus` in its result ever since that fix, but `gmCompleteQuest` in src/renderer.js never read it, so completing the last daily quest in a category silently granted +10 XP with zero UI feedback (the XP toast only showed `res.xpAwarded`, undercounting what the profile total actually jumped by). Added a toast (`🧹 Category swept! +N XP bonus`) alongside the existing badge/freeze toasts. `node --check` passes on all four core files.
- 2026-07-21: fixed a freeze-token leak in `uncompleteQuest` (src/db.js) — when `completeQuest` bridges a 1-day gap by spending a freeze token, undoing that same completion reverted the streak/last_completion_date but never refunded the token, so a complete→uncomplete cycle permanently burned it for nothing (the streak effect it paid for was being undone in the same operation). Added a `freeze_used` column to `quest_completions` (migration v4), set it when a completion consumes a token, and refund the token (capped at 3) in `uncompleteQuest` when that completion is reverted. Verified against a real SQLite DB (Node's built-in `node:sqlite` via a `better-sqlite3`-API shim, since that package isn't installed in this environment): bridge consumes the token and records `freeze_used=1`, undo refunds the token and restores the true prior `last_completion_date`. `node --check` passes on all four core files.
- 2026-07-21: fixed a farmable category-sweep bonus in `completeQuest` (src/db.js) — the +10xp sweep bonus was granted any time `catDoneToday === catDailyTotal` at completion time, with no record of whether it had already fired that day, and `uncompleteQuest` never claws it back (by design — it removes only the individual quest's own xp). Net effect: completing the last daily quest in a category, uncompleting it, then recompleting it re-satisfied the same-count condition and re-granted +10xp every cycle — unlimited free xp with zero real progress (same farming shape as the already-fixed weekly-quest bug, just in the sweep path). Fixed by recording `source_id=category_id` on `category_sweep` xp_log rows and checking for an existing sweep entry for that category on the app-date (rollover-hour-shifted, matching the rest of the app-date logic) before granting. Verified against a real SQLite DB (`node:sqlite` shim): first completion of the last daily quest in a category grants the bonus once (sweepBonus:10); 5 subsequent uncomplete→recomplete cycles on that same quest all return sweepBonus:0 and total XP stays flat, confirming the farm is closed. `node --check` passes on all four core files.
- 2026-07-21: fixed the documented "weekly quest can't be un-ticked later in the week" quirk in `uncompleteQuest` (src/db.js) — it only ever looked up a completion row for *today's* app_date, but weekly quests can be completed on any day of the ISO week (`listQuests`/`completeQuest` treat the whole `%Y-%W` week as "done"). Unticking a weekly quest completed earlier in the week silently no-op'd (`{xpRemoved:0}`, no error) and the checkbox just snapped back on the next render. Fixed by looking up the quest's actual completion row for the current ISO week (falling back to the exact-date lookup for daily/epic, unchanged), deleting by row id instead of `(quest_id, app_date)`, and reverting the category streak/freeze-token state against the date the completion actually happened on (`completionDate`) rather than today's date, since that's the date whose streak effect needs undoing. Verified against a real SQLite DB (`node:sqlite` shim): daily complete→uncomplete regression still passes; a weekly quest completed and then relocated (via raw SQL) to Monday of the same week is found and removed by `uncompleteQuest` called "today", `listQuests` correctly flips back to not-completed, and the category streak/last_completion_date revert to their pre-completion state. `node --check` passes on all four core files.
- 2026-07-21: fixed a shop double-spend in the "Streak Freeze" consumable purchase — `buyShopItem('freeze_token')` in src/renderer.js did a client-side `coins.get()` balance check, then two separate non-atomic IPC round-trips (`coins:award` to deduct, `streaks:add-freeze` to grant), unlike the cosmetic-item path (`purchaseItem` in db.js) which checks-and-deducts atomically inside one SQL transaction. Two overlapping purchase clicks (double-click / held Enter) could both pass the stale balance check before either deduction landed, granting multiple freeze tokens for the price of one — same double-spend shape as the already-fixed weekly-quest and sweep-bonus farms. Added `purchaseFreeze(cost)` to db.js (single transaction: check balance, deduct, grant +1 token to every category, capped at 3), wired through a new `shop:purchase-freeze` IPC handler (main.js) and `shop.purchaseFreeze` preload bridge, and switched the renderer's consumable branch to call it instead of the two-step award+addFreeze. Verified against a real SQLite DB (`node:sqlite` shim): insufficient-balance purchase is rejected with no token grant; a normal purchase deducts once and grants +1 per category; simulating the race (two sequential calls against a balance that covers only one) now correctly rejects the second call instead of double-granting. `node --check` passes on all four core files.
- 2026-07-22: re-audited db.js/main.js/preload.js/renderer.js for remaining gaps now that all bugs/v1.1 items/code-quality passes are done; found no new correctness bugs but caught leftover dead code from the 2026-07-21 fix — `coins:award` and `streaks:add-freeze` (plus their db.js functions `awardCoins`/`addFreezeTokens` and preload bridge entries) were the old non-atomic freeze-purchase path's two IPC handlers, made obsolete when `purchaseFreeze` replaced their only caller, but were left wired up and callable directly, bypassing the atomic-transaction guarantees the last several fixes established (unrestricted coin grants, non-atomic freeze grants). Confirmed via grep they were unreferenced anywhere in renderer.js, then removed them from all four files. `node --check` passes on all four core files.
- 2026-07-22: fixed a shipping regression in main.js — `mainWindow.webContents.openDevTools()` ran unconditionally on every launch instead of only in dev mode. `git log -p` showed it was previously gated behind `if (!app.isPackaged)` (present since the original electron-updater commit) but that guard was dropped in the game-rebuild commit, so a packaged install would pop the Chromium DevTools panel open for the end user on every start. Restored the `!app.isPackaged` guard. `node --check` passes on all four core files.
- 2026-07-22: closed an input-validation gap in the shop IPC path — `shop:purchase`/`shop:purchase-freeze` (main.js) only checked that `cost` was a finite number, and `purchaseItem`/`purchaseFreeze` (src/db.js) silently clamped a negative cost to 0 via `Math.max(0, ...)`, so a negative cost bought any item (or a freeze token) for free instead of being rejected. Every real caller in renderer.js passes `item.cost` straight from the hardcoded `SHOP_ITEMS` catalog (always ≥0), so this wasn't reachable through normal play, but it's a real gap for anyone driving the IPC bridge directly (e.g. devtools console) on this single-user app. Added a `cost < 0` rejection at the IPC boundary in main.js and made db.js reject (`{ok:false, error:'invalid_cost'}`) rather than clamp; `cost:0` (the several free catalog defaults) remains valid. `node --check` passes on all four core files.
- 2026-07-22: removed a second, fully dead identity system found while re-auditing — the SQLite `profile.display_name` column, `db.updateDisplayName()`, the `profile:update-name` IPC handler (main.js, with its own input validation), and the `profile.updateName` preload bridge were never called from anywhere in src/renderer.js. The app's actual display name is `st.userName`, a separate localStorage-backed value wired through onboarding, the greeting, sidebar, AI system prompt, and notifications (src/renderer.js:203,247,305,583,685,839,1280,1499,1548,1556,2329) — `profile.displayName` (returned by `getProfile()`) is never read anywhere, so the update path was unreachable dead code, same shape as the `coins:award`/`streaks:add-freeze` cleanup earlier today. Removed the write path (handler/bridge/db function); left `profile.display_name`/`getProfile().displayName` in place since it's a harmless read-only column and removing it would need a schema migration for no behavioral gain. `node --check` passes on all four core files.
- 2026-07-23: fixed a deeper, still-live instance of the app-date/real-date mixing bug class (#4) that the earlier rollover fixes hadn't reached — `getAppDate()` derived the rollover decision from LOCAL time (`now.getHours()`/`now.setDate()`) but serialized the result with `now.toISOString()`, which formats in UTC. For any timezone where local and UTC calendar days differ (true for part of every day everywhere except UTC+0), this silently shifts the app-date by an extra day. Reproduced with a direct simulation: at 00:30 local time in Europe/Lisbon (the dev machine's own zone, currently UTC+1 for BST/WEST) with the default 4am rollover, the old code returned `2026-07-21` when the correct app-date is `2026-07-22` — wrong by a full extra day, live every night in the ~1-hour window before 1am. The same UTC/local mismatch pattern (`new Date(dateOnlyString)`, parsed as UTC midnight, then mutated with local `getDate()/setDate()`) was also present in `completeQuest`'s yesterday/day-before-yesterday calc, `getXpHistory`'s day-anchor loop, and `getStreaks`' global-streak day-walk. Added `formatLocalDate()`/`parseLocalDate()` helpers (local calendar-day string ⇄ local-midnight Date, no UTC round-trip) and switched all four call sites to them; left the one `new Date(dateStr)` subtraction in `getStreaks`' longest-streak calc alone since it only diffs two UTC-parsed instants (never mutated/reserialized), which is timezone-safe as-is. Verified the fix against the reproduction above (now correctly returns `2026-07-22`) and confirmed `node --check` passes on all four core files. `better-sqlite3` isn't installed in this environment so the full `completeQuest`/`getXpHistory`/`getStreaks` paths weren't re-run end-to-end this pass; the fix is localized to date-formatting-only helper swaps with no logic change.
- 2026-07-23: found and fixed one more instance of the same app-date/real-date mixing class (#4) that the same-day fix above didn't reach — it fixed the JS-side `Date` arithmetic, but two raw SQL queries in db.js independently compute an app-date from `xp_log.occurred_at` via `date(occurred_at, '-N hours')`: the `getXpHistory` bar-grouping query and `completeQuest`'s category-sweep-bonus `alreadySwept` dedup check. `occurred_at` is stored in UTC (SQLite's `datetime('now')`), but that SQL shifts it by the rollover hours only, with no timezone conversion — so it's silently computing a UTC-calendar-day-shifted-by-rollover, not a local-calendar-day-shifted-by-rollover like `getAppDate()` does. For any non-UTC timezone this disagrees with the rest of the app during a window (the size of the UTC offset) around the rollover hour. Reproduced against a real SQLite DB (`node:sqlite`) with `TZ=Europe/Lisbon`: a completion at local 04:30 BST (just after the 4am rollover, UTC 03:30) bucketed to `2026-07-21` under the old query vs. the correct `2026-07-22` that `getAppDate()` computes for the same instant. In that window, `getXpHistory` could mislabel which day's bar XP lands on, and the sweep-bonus dedup check could disagree with the `appDate` key it's compared against (re-granting or wrongly blocking the +10xp bonus). Fixed by adding SQLite's `'localtime'` modifier before the rollover-hour shift in both queries, so they convert UTC→local before applying the same shift `getAppDate()` uses. Verified against a real SQLite DB (`node:sqlite`) under `TZ=Europe/Lisbon`: `completeQuest`/`getXpHistory` run end-to-end, and the raw `date(occurred_at, 'localtime', '-4 hours')` now matches `getAppDate()`'s local-time result where the old `date(occurred_at, '-4 hours')` was off by a day. `node --check` passes on all four core files.
- 2026-07-23: fixed a renderer-side instance of the same UTC/local mixing bug class (#4) in `src/renderer.js` — db.js timestamps (`completed_at`, `unlocked_at`) come from SQLite's `datetime('now')` as `"YYYY-MM-DD HH:MM:SS"` UTC with no timezone marker, and `new Date(str)` on that exact non-ISO (space-separated) format is parsed by V8 as **local** time rather than UTC, so every such timestamp was silently off by the local UTC offset. Concretely, `_timeAgo(new Date(a.completed_at))` in the Recent Activity feed always showed activity as further in the past than it actually was (verified: a completion 5 minutes ago showed "1 hr ago" under `TZ=Europe/Lisbon`, off by exactly the UTC+1 offset), and the Achievements grid's badge-unlock tooltip date (`b.unlocked_at.split(' ')[0]`) showed the raw UTC calendar date instead of the local one. Added a `parseUtcTimestamp()` helper (swaps the space for `T` and appends `Z` so it parses as real ISO-8601 UTC) and switched both call sites to use it. Verified with a standalone reproduction under `TZ=Europe/Lisbon`: old code returned "1 hr ago" for a 5-minutes-ago timestamp, fixed code correctly returns "5 min ago". `node --check` passes on all four core files.
- 2026-07-23: fixed a logic bug (separate from the #4 date-*format* class, but same "app-date vs today" family) in `getStreaks()` (src/db.js) — the "Global day streak" widget (`gm-streak-num`/`game-gstreak`, the most prominent streak number on Home and the Game tab) computed the streak by walking backward from *today's* app-date and breaking immediately if today has no completion yet, so it showed **0 every single morning** for every user with an otherwise-intact streak, even though nothing had actually broken (per-category `current_streak` correctly persists at its last value until the next completion recalculates it — only the global number reset prematurely on every read). Fixed by anchoring the walk at the most recent activity date when that date is today *or yesterday* (mirroring `completeQuest`'s own "1-day gap is still alive, 2+ days is broken" threshold), and only reporting a broken (0) streak when the most recent activity is truly 2+ days stale. Verified with a standalone simulation of the walk logic: active-streak-through-yesterday-with-nothing-done-today now correctly reports the prior streak length instead of 0; a genuinely stale (2+ day gap) history still correctly reports 0; today-already-completed and mid-history-gap cases are unchanged. `node --check` passes on all four core files.
- 2026-07-24: re-audited db.js/main.js/preload.js and the game-related renderer.js paths again (bugs, v1.1 items, IPC validation) — found nothing newly broken; the game system has been thoroughly covered by prior passes. Found one real robustness gap instead, in `migrate()` (src/db.js): each migration ran its DDL via `_db.exec(m.sql)` and was only recorded into `schema_migrations` *after* it succeeded, but `exec()` wasn't wrapped in an explicit transaction — if a future migration ever threw partway through, the partially-applied schema would persist un-marked, and the next launch would retry the identical SQL from scratch, crashing forever on non-idempotent statements (e.g. `ALTER TABLE ADD COLUMN` → "duplicate column"). Wrapped each migration's DDL + its `schema_migrations` insert in one `_db.transaction()` so a failure cleanly rolls back to nothing-applied, letting the next launch retry safely. Verified against a real SQLite DB (`node:sqlite` shim): `initDB` still runs all 4 existing migrations end-to-end with correct data (27 quests seeded, profile default state) and is idempotent on a second `initDB` call against the same file; separately confirmed the transaction wrapper actually rolls back a deliberately-failed multi-statement `exec()` (0 rows survive) rather than leaving a partial write. `node --check` passes on all four core files.
- 2026-07-24: fixed a sweep-bonus/level-up ordering bug in `completeQuest` (src/db.js) — level, `leveledUp`, `coinsFromLevelUp`, and the level_5/10/20 badge check were all derived from the total XP *before* the +10xp category-sweep bonus was added, even though the sweep bonus (computed later in the same call) can itself be what crosses a level threshold. Since `getProfile()` always derives level fresh from `total_xp` (never reads the stored `current_level` column), a sweep-triggered level-up was never merely delayed — it was permanently skipped: no level-up toast, and the `coinsFromLevelUp` reward for that level was lost for good, because the very next call already sees a post-sweep `oldXp` that `levelForTotalXp` reports as already past the threshold, so the transition can never be observed again. Fixed by moving the sweep-bonus computation before the level/coins derivation and the level-badge check, so both are always computed against the final (post-sweep) total in one pass, with a single profile UPDATE at the end. Verified against a real SQLite DB (`node:sqlite` shim): a synthetic case where 4 ordinary health-quest completions leave the profile 5xp under the level-2 threshold, and the 5th (which also completes the category, firing the sweep bonus) pushes it over — the last completion now correctly reports `leveledUp:true`, `newLevel:2`, `coinsFromLevelUp:20`; a control case (level-up from quest XP alone, no sweep involved) still behaves exactly as before. `node --check` passes on all four core files.
- 2026-07-24: found and fixed a year-boundary instance of the weekly-quest-farming bug class (#2) that the original fix didn't fully close — `listQuests`/`completeQuest`/`uncompleteQuest` (src/db.js) all bucketed weekly-quest completions using SQLite's `strftime('%Y-%W', app_date)`, which numbers weeks from Jan 1 of each year rather than as true continuous Mon-Sun calendar weeks. Whenever a year boundary falls mid-week, the days on either side get different `%Y-%W` keys despite being in the same real 7-day week: reproduced against a real SQLite DB (`node:sqlite`) that the actual Mon Dec 29 2025 - Sun Jan 4 2026 week splits into `"2025-52"` (Dec 29-31) and `"2026-00"` (Jan 1-4) under the old scheme, so a weekly quest completed Dec 30 2025 could be completed again as early as Jan 1 2026 — a 2-day gap, not a 7-day one. Fixed by replacing all three `strftime('%Y-%W', ...)` comparisons with a `isoWeekRange()` helper that computes the true local Mon-Sun date range containing the app-date and compares via `app_date BETWEEN weekStart AND weekEnd`, sidestepping year-boundary week-labeling entirely. Verified against a real SQLite DB: the Dec 30/Jan 1 scenario above is now correctly blocked (previously allowed); a full `completeQuest`→blocked-recomplete→`uncompleteQuest`→`completeQuest` cycle on a real weekly quest (via `listQuests`) still behaves exactly as before for the non-boundary case. `node --check` passes on all four core files.
- 2026-07-24: re-audited db.js/main.js/preload.js/renderer.js again (bugs list, v1.1 items, IPC validation) — all confirmed already fixed/complete, found no new correctness bugs in the gamification logic itself. Found one real error-handling gap instead, in `youtube-oauth-start` (main.js): the local loopback HTTP server used for the OAuth redirect had no `'error'` listener, so a `server.listen()` failure (port/socket error) would emit an unhandled `error` event — Node treats that as an uncaught exception rather than settling the pending promise, leaving the renderer's `await` hanging for the full unrelated 2-minute timeout with no indication of what actually went wrong. Added `server.on('error', ...)` to clear the timeout and resolve with a descriptive error immediately. `node --check` passes on all four core files.
- 2026-07-25: re-audited db.js/main.js/preload.js line-by-line again (bugs, v1.1, IPC validation) — found nothing new; those three files are now genuinely well-covered by ~20 prior passes. Shifted focus to the previously-lighter-touch game-related renderer.js code (`gmCompleteQuest`, `rGame`, `rShop`/`buyShopItem`/`equipShopItem`/`applyEquips`) and found a minor race: `gmCompleteQuest` had no in-flight guard, so a rapid re-click on a quest card before the first request's re-render lands fires a second `complete()`/`uncomplete()` call for the same quest — the DB itself blocks the duplicate (UNIQUE(quest_id,app_date) for daily, explicit "already completed this week" check for weekly) so no XP/coin corruption was possible, but the duplicate call surfaced as a raw error caught by the generic handler, showing an unhelpful "Could not update quest" toast instead of just being ignored. Added a `questInFlight` Set guard around `gmCompleteQuest` so a second click on the same quest while one is pending is a no-op. `node --check` passes on all four core files. Note to Daniel: the bug list, v1.1 queue, and IPC/error-handling code-quality pass are now all complete and have been re-verified clean across several consecutive days of passes with only minor/cosmetic residue turning up — the substantive queue in this roadmap is exhausted. Remaining work is the v1.2+ list (real API integrations, installer polish, data export) or the merge-to-main decision itself, neither of which this autonomous routine is scoped to do unprompted. Worth considering pausing/reducing this routine's cadence, or explicitly greenlighting a v1.2+ item, next time you're at the keyboard.
- 2026-07-25: second pass today, covering ground the prior 20+ audits hadn't reached — the new sychboard-mcp/mcp-client.js "AI OS" integration (spawns a stdio MCP subprocess, runs SSH commands and filesystem walks on the LLM's behalf) and the two-layer permission enforcement it depends on. Traced the full path: `permissions.json` → `mcp-client.js` (`callTool` refuses anything not `auto`/`confirm`, and refuses `confirm`-tier calls without `approved===true`) → `sychboard-mcp/server.js`'s own `requirePermission()` re-checks the same file server-side, so even a compromised/buggy renderer can't bypass approval by lying about `approved`. `list_recent_project_files`' path traversal guard (must resolve inside `os.homedir()`) and depth cap (8) both hold up; `get_chuck_bird_status`'s SSH command has no attacker-controlled interpolation (host/service come from hardcoded consts/env, not tool args). Also re-checked `main.js`'s `mcp:list-tools`/`mcp:call-tool` handlers (already validate `name`) and `preload.js`'s bridge — no gaps found. Deliberately left the boot-screen code alone even though the latest commit (`Boot: strip stars and orb entirely, pending new design direction`) removes the Three.js orb — that's Daniel iterating on the design directly (own commit, explicit WIP note), not a bug, so out of scope for this routine to touch. No code changes this pass; confirms the same conclusion as the first pass today: nothing actionable remains in the bugs/v1.1/code-quality queue, including in the newest subsystem. `node --check` passes on all four core files (no logic changed, doc-only commit).
- 2026-07-25: third pass today — followed up on the previous pass's MCP audit by checking the renderer-side half it hadn't reached yet (`getMcpTools`/`runMcpToolCall`/`askToolApproval` in src/renderer.js, added the same day in "Wire sychboard-mcp into the in-app AI Assistant panel"). Found a real bug in `getMcpTools()`: it memoized `_mcpTools` on *any* outcome, including a failed `mcp:list-tools` call (server not yet spawned, transient IPC error, etc.) — `_mcpTools` was set to `[]` in that case and never reset anywhere else in the file, so one transient failure at first use permanently disabled every live tool (Chuck Bird status, system info, project files) for the rest of the renderer's lifetime, even though `mcp-client.js`'s own `ensureStarted()` correctly resets its `readyPromise` and would happily retry on the very next call. Fixed by only caching `_mcpTools` on a genuine success (`r.ok===true`); a failure now returns `[]` for that one call without memoizing, so the next chat message retries `listTools()` fresh. Confirmed by reading (not re-implementing) the two possible cache-then-return paths pre/post fix — no DB involved, pure renderer logic, so no `better-sqlite3`/`node:sqlite` simulation needed here. `node --check` passes on all four core files.
- 2026-07-26: full re-audit pass across all four priority tiers — bugs list, v1.1 queue, and IPC/error-handling code quality all confirmed still fixed/complete, no regressions. Went deepest this pass on the two least-covered subsystems: `sychboard-mcp/server.js` (re-read `getChuckBirdStatus`'s SSH command construction, `listRecentProjectFiles`'s home-dir containment check and depth cap, `getSystemStatus`'s Windows-only `powershell.exe` disk query wrapped in try/catch) and `mcp-client.js` (spawn/teardown/reconnect flow, `rpc()` pending-map timeout handling) — no injection paths, no unhandled rejections, no stale-state bugs found. Also re-verified the shop/equip system end-to-end (`SHOP_ITEMS`/`buyShopItem`/`equipShopItem`/`applyEquips`/`purchaseFreeze`) and the quest-completion UI flow (`gmCompleteQuest`'s `questInFlight` guard, toast/badge/level-up sequencing) — both match their DB-side contracts with no gaps. No new code changes this pass; the queue remains genuinely exhausted outside the merge-to-main decision and the deferred v1.2+ list.
- 2026-07-26: second pass today — with bugs/v1.1/IPC-validation all genuinely exhausted (per the ~20 prior audits), widened scope to code quality in the least-audited area: the original localStorage-backed features (habits/goals/todos/custom sections) and the AI assistant's `executeActions` handler in src/renderer.js. Found a real, consistent stored-HTML-injection gap: the codebase already has a `sanitizeText()` escaping helper used at write-time for shifts/subscriptions/trips/events/API keys, but habit/goal/todo/fitness-goal/custom-section-tracker text, custom-section names (add/rename), and every free-text field the AI assistant's `[ADD_HABIT:...]`/`[ADD_GOAL:...]`/`[ADD_TODO:...]`/`[ADD_TRIP:...]`/`[ADD_SUBSCRIPTION:...]`/`[ADD_EVENT:...]` tags populate were stored raw and then interpolated unescaped into `innerHTML` template strings by their render functions (`rHabits`, `rGoals`, `rSecTodos`/`rMasterTodos`, `rCustom`, sidebar/home-card/manage-list section labels). Typing (or having the assistant echo) something like `<img src=x onerror=...>` as a habit/goal/todo/section name would execute as real markup/script in the renderer's DOM on next render — a genuine local code-execution vector in an Electron app, not just cosmetic. Fixed by applying the existing `sanitizeText()` convention at all of these write sites (`addHabit`, `addFitnessGoal`, `addGoal`, `addSecTodo`, `addGenTodo`, `addCsTodo`, `addCsTracker`, `doAddSec`, `doRename`, and the corresponding branches of `executeActions`), matching what `addShift`/`addSub`/`addTrip`/`addEvent` already did. Left `set_focus`/`set_yt_channel`/`set_exam_date` alone — verified their values render via `textContent`/`.value`, not `innerHTML`, so they were never actually exploitable. `node --check` passes on all four core files.
- 2026-07-26: third pass today — followed up on the previous pass's stored-HTML-injection sweep by checking two fields it hadn't reached: Dev/side-project todos and custom-section notes (`src/renderer.js`). `addDevTodo` stored its input raw (no `sanitizeText`) and `rDev()` interpolates `t.text` unescaped into a `<span>` via `innerHTML` — same exploitable shape as the habits/goals/todos fixed last pass. Custom-section notes (`cs.notes`, `rCustom()`'s notes/freeform branch) were worse: written raw from the textarea's `.value` with no escaping at all, then re-injected raw inside a `<textarea id="cs-n-${id}">${cs.notes}</textarea>` via `innerHTML` — a note containing `</textarea>` closes the real textarea early and any markup/script after it becomes live DOM, not just inert text, a stronger break than the span-based cases (`<script>` after a real `</textarea>` executes; `<script>` inside a `<span>` does not). Confirmed `dev.notes` itself was already safe (rendered through the HTML-escaping `parseMD()`, not raw) so only the todos list needed a fix there. Applied `sanitizeText()` at both write sites (`addDevTodo`, the inline notes-save handler in `rCustom`, generous 5000-char cap for notes vs. the usual 200) — entities round-trip correctly through the existing `innerHTML`-based render (browsers decode `&lt;`/`&gt;`/`&quot;`/`&#39;` back to literal characters when parsing textarea/span content), matching the same pattern already used for the other fields. `node --check` passes on all four core files.
- 2026-07-26: fourth pass today — cross-checked every remaining `.push(`/`.innerHTML=` site in `src/renderer.js` against the `sanitizeText()` sweep from the last two passes to find any write path still missed. Found one: `addSchedEvent()` (the Schedule tab's own "add event" input, distinct from the Home widget's `addEventFromModal()`) wrote its text straight from the input's `.value` with no sanitization into `st.scheduleEvents[day]`, which both `rSchedule()` (`${e.t}`) and `rSchedEvents()` (`${ev.t}`) later interpolate unescaped into `innerHTML` — same stored-injection shape as the habits/goals/todos/notes fields fixed the last two passes, just on the sibling code path that writes the identical data structure. `addEventFromModal()` already sanitized correctly, which is likely why this one was missed in the broader sweep. Fixed by wrapping the input read in `sanitizeText(...,60)`, matching the modal path's own cap. `node --check` passes on all four core files.
- 2026-07-27: fifth pass on the stored-injection sweep — checked the one write path the previous four passes hadn't reached: the AI assistant's `executeActions()` handler in `src/renderer.js` (all `[ADD_*:...]`/`[SET_*:...]` action tags the LLM's replies get parsed into). Every text-bearing action already routes through `sanitizeText()` (todo/habit/goal/trip/subscription/event) except `add_shift`: `parseActions()`'s `[ADD_SHIFT:date:hours:wage]` regex captures the date field as a raw trimmed string, and `executeActions()` pushed it straight into `st.shifts` with no escaping, while the manual "Add Shift" form (`addShift()`) does sanitize the same field. `rShifts()` then interpolates `s.date` unescaped into a `<span>` via `innerHTML` — the identical exploitable shape as the habits/goals/todos/notes/events bugs fixed in the prior four passes, just on the shift-logging action tag those sweeps missed. Fixed by wrapping `a.date` in `sanitizeText(a.date,50)` at the `add_shift` branch, matching the manual form's own 50-char cap. Re-checked `set_focus`/`set_yt_channel`/`set_exam_date` again — still confirmed safe, they render via `textContent`/`.value`, not `innerHTML`. `node --check` passes on all four core files.
- 2026-07-27: sixth pass on the stored-injection sweep, and found the `set_exam_date`/`examDate` re-check from the prior pass was wrong — it only traced `examDate`'s dedicated display span (`exam-date-disp`, `textContent`, safe) and missed that the Home dashboard's `sums.uni` string (`` `Exam: ${st.examDate}` `` at src/renderer.js:812) is later interpolated unescaped into `home-grid`'s `innerHTML` (the `hc-value` div), the same home-page grid every other section's summary renders through. Both write paths stored `examDate` raw: the AI assistant's `set_exam_date` action tag (`executeActions()`) and the manual "Uni" tab date input (`updateExamDate()`). Fixed by wrapping both with `sanitizeText(...,50)`, matching the field's existing display cap. `node --check` passes on all four core files.
- 2026-07-27: same pass, second find — the sweep had only ever grepped `src/renderer.js` for raw write sites, missing that `src/index.html` also has inline `onclick` handlers writing directly into `st.*`. The Settings tab's "Your name" save button wrote `document.getElementById('name-in').value` straight into `st.userName` with no sanitization (the onboarding name field, a sibling write path for the same state, already sanitized correctly). `st.userName` is interpolated unescaped into `innerHTML` in `rAI()` (`src/renderer.js:1435`, the AI tab's empty-chat greeting bubble), trivially re-triggerable via the existing `clearChat()` — same exploitable shape as every other fix in this sweep. Fixed by wrapping the Settings-tab write in `sanitizeText(...,100)`, matching the onboarding field's cap. `node --check` passes on all four core files (index.html isn't JS; verified the edited `onclick` attribute is well-formed by inspection).
- 2026-07-27: seventh pass — swept every `.push`/write site feeding `innerHTML` across `src/renderer.js` for anything the six prior stored-injection passes missed. Found one: `executeActions()`'s `add_goal` branch (the AI assistant's `[ADD_GOAL:text|category]` action tag) sanitized the goal's `text` field but stored `category:a.cat` completely raw, while the manual "Add Goal" form's category field is a constrained `<select>` dropdown and was never the risk. `rGoals()` interpolates `${g.category}` unescaped directly into `innerHTML` (`src/renderer.js:2001`, both the CSS-class lookup and the visible chip label), so a category value smuggled through the AI action tag — not reachable via the manual form's dropdown, only via the LLM's free-text tag parsing — could inject markup into the Goals list on next render, same exploitable shape as the five prior `sanitizeText()` fixes in this sweep. Fixed by wrapping it in `sanitizeText(a.cat,20)`, matching the field's short display length. `node --check` passes on all four core files.
- 2026-07-27: eighth pass — cross-referenced every `innerHTML=` site in `src/renderer.js` against its write path once more (prior passes had covered todos/habits/goals/notes/events/shifts/exam-date/username/dev-todos, i.e. everywhere `sanitizeText` was already used) and found one write site the sweep had never reached: `logSleep()`'s free-text "note" field (Sleep tab) stored `document.getElementById('sleep-note').value` completely raw into `st.sleep.logs[].note`, while `rSleep()` interpolates `l.note` unescaped into a `<span>` via `innerHTML` (src/renderer.js:2231, the sleep log list) — identical exploitable shape to every other fix in this sweep. The AI assistant's `log_sleep` action tag was already safe (hardcodes `note:'via AI'`, no user text). Fixed by wrapping the read in `sanitizeText(...,100)`. `node --check` passes on all four core files.
- 2026-07-28: ninth pass, no changes — independently re-verified the whole scope from scratch (bugs list, v1.1 queue, main.js/preload.js/db.js in full, and every remaining `.value`→state write site in renderer.js/index.html against its render path) plus a fresh direct read of sychboard-mcp/server.js and mcp-client.js (previously only re-audited by earlier passes, not re-read by this one). Found one inconsistency worth noting but not fixing: `set_yt_channel`'s AI action tag and the YouTube API's `chan.snippet.title` response both write `st.yt.channelName` without the `sanitizeText()` wrapper every sibling field in `executeActions()` now has — but `channelName` is only ever rendered via `textContent` (`yt-chan-title`, src/renderer.js:1726), never `innerHTML`, so unlike the eight fixes above there's no actual injection path today; sanitizing it now would be validating a scenario that can't currently happen, so left as-is. No reachable bugs, unescaped-`innerHTML` gaps, or IPC/error-handling holes found anywhere else. The bugs/v1.1/code-quality queue remains genuinely exhausted; only the merge-to-main decision and the deferred v1.2+ list remain.
- 2026-07-28: tenth pass (second today) — re-read db.js/main.js/preload.js/sychboard-mcp end-to-end again plus swept every remaining `st.*=`/`innerHTML=` write site in renderer.js and index.html (found two more raw textarea writes, `st.todayFocus`/`st.fitnessNotes` — confirmed safe, same as the `set_yt_channel` case above: only ever rendered via `.value`/plain-text AI-prompt interpolation, never `innerHTML`). Found and fixed a real input-validation gap instead, in the shop IPC path: `purchaseItem(itemKey, cost)`/`purchaseFreeze(cost)` (src/db.js) validated that the renderer-supplied `cost` was a non-negative finite number, but never checked it was the *correct* price for that item — the actual catalog (name/cost per key) only ever existed client-side in `SHOP_ITEMS` (src/renderer.js). Anyone driving the IPC bridge directly (devtools console — same threat model as the negative-cost gap fixed 2026-07-22, just for the price's magnitude rather than its sign) could call `shop.purchase('accent_gold', 0)` and buy a 100-coin cosmetic for free, or invent a nonexistent `itemKey` and "own" it for 0 coins (including `'freeze_token'`, which is only meant to go through the separate `purchaseFreeze` path). Not reachable through normal play — every real caller in renderer.js passes the hardcoded catalog's own `item.cost` — but a real gap for direct IPC access on this single-user app, same shape as the fix two weeks ago. Fixed by adding a `SHOP_CATALOG` (key→cost) and a `FREEZE_TOKEN_COST` constant in db.js as the server-side source of truth: `purchaseItem` now looks up the price itself and rejects any `itemKey` not in the catalog (`{ok:false,error:'unknown_item'}`, which also naturally blocks `'freeze_token'` from this path since it's deliberately excluded from `SHOP_CATALOG`); `purchaseFreeze` no longer takes a `cost` argument at all and always charges 150. Verified against a real SQLite DB (`node:sqlite` shim): buying `accent_gold` with a forged cost of 0 still correctly charges the real 100; an unknown item key is rejected regardless of cost; `freeze_token` via `purchaseItem` is rejected; `purchaseFreeze` charges the real 150 even when called with a forged 0; re-buying an already-owned item is still rejected. `node --check` passes on all four core files.
- 2026-07-28: eleventh pass (third today) — the previous pass's `purchaseFreeze` fix (src/db.js) dropped its `cost` parameter but left main.js's `shop:purchase-freeze` IPC handler, preload.js's bridge, and the `buyShopItem` call site in src/renderer.js all still threading a `cost` argument through to it. JS silently drops the now-unused extra argument rather than erroring, so nothing crashed or misbehaved, but the IPC handler kept validating (`isFiniteNumber(cost) || cost<0`) a value that no longer had any effect on the purchase — stale plumbing left over from the refactor, undermining the point of moving pricing server-side. Removed the parameter from all three call sites (`main.js`'s handler now takes no args and just calls `db.purchaseFreeze()`; `preload.js`'s `purchaseFreeze()` bridge takes none; `renderer.js`'s `buyShopItem` calls it with no argument) so the whole chain matches db.js's real signature. Confirmed `isFiniteNumber` is still used elsewhere in main.js (`shop:purchase`, `xp:history`), so no dead helper left behind. `node --check` passes on all four core files.
- 2026-07-28: twelfth pass (fourth today), no changes — did a fresh, independent line-by-line read (not just a diff against prior summaries) of main.js, preload.js, src/db.js, mcp-client.js, and sychboard-mcp/server.js in full, plus spot-checked the boot-screen teardown path (`startBoot`/`disposeBootThree`) since the 2026-07-19 window-resize fix predates the 2026-07-25 "strip stars and orb" commit that removed the Three.js renderer that fix targeted — confirmed the strip commit already cleans up `bootResizeHandler`/`bootMouseHandler`/`bootOrbAnim` correctly (nulled and unregistered at both `startBoot()` entry and `enterApp()`), so no dead-handler regression there. Cross-checked `SHOP_ITEMS` (renderer.js) against `SHOP_CATALOG`/`FREEZE_TOKEN_COST` (db.js) — prices still in sync. Found nothing new in any of these files. **Assessment for Daniel:** this is the 4th consecutive day (and this session's 12th+ pass overall) of full re-audits turning up only increasingly marginal or purely cosmetic findings (stale unused parameters, dead code from earlier refactors) rather than real bugs — the bugs list, v1.1 queue, and code-quality pass have been genuinely exhausted since roughly 2026-07-25, which this and the intervening passes have each independently reconfirmed. Continuing to fire this routine at its current cadence has low marginal value from here. Recommend either: pausing/slowing this routine, redirecting it at a specific v1.2+ item (real API integrations, installer polish, data export/backup), or making the merge-to-main call yourself so the routine's remaining scope (bugs/v1.1/code-quality) closes out for good.
- 2026-07-28: thirteenth pass (fifth today), first v1.2+ item — Daniel greenlit moving into the v1.2+ list live, in ease/self-containment order. Picked **data export/backup** first since it's fully self-contained (no external API keys/live testing needed) and, on inspection, turned out to be an existing gap rather than greenfield work: Settings already had a "Data Export" button (`exportData()` in renderer.js) labeled as "a local backup of all your SychBoard data," but it only ever dumped the `sb4_*` localStorage keys (finance/habits/journal/etc.) — it silently omitted the entire SQLite gamification layer (XP, level, SychCoins, streaks, badges, quest history), and there was no restore/import path at all. Implemented both halves: `db.js` gained `exportGameData()`/`importGameData(data)` (the latter transactional, shape-validated, rejects and rolls back atomically on malformed input or a foreign-key violation rather than partially applying, re-derives `current_level` from `total_xp` rather than trusting the stored value, restores default settings — `day_rollover_hour`/`soft_reset_enabled`/`shop_owned` — if the backup predates them); new `data:export-game`/`data:import-game` IPC handlers in main.js and a `sychboard.data.{exportGame,importGame}` preload bridge; `exportData()` in renderer.js is now async and merges the game-data export in with the localStorage dump into one versioned `{sychboard_backup:true,...}` JSON file; added a new "Restore from Backup…" button + hidden file input + `triggerImportData()`/`handleImportFile()` that parse the file, confirm via the existing `showConfirm()` modal (full-overwrite warning), then replace all `sb4_*` keys and call the game-import IPC before reloading. Verified against a real SQLite DB (`node:sqlite` shim, since `better-sqlite3` isn't installed here): completed quests across two categories including a weekly quest and a category sweep, bought a shop item, exported, then imported into a fresh empty DB — total XP (185), coins, per-category streak, and the weekly quest's "completed this week" state all matched exactly; a malformed payload (missing required arrays) is rejected with the DB left completely untouched; a payload referencing a quest_id that doesn't exist in the target DB (simulating a corrupted/foreign file) fails the *entire* import atomically via the FK constraint + transaction rollback rather than silently dropping just that row, leaving zero partial state. `node --check` passes on all four core files (index.html reviewed by inspection, not JS).
- 2026-07-29: fourteenth pass, second v1.2+ item — picked the first slice of "ADHD loop tuning" (a 3-part bullet: nudge/confetti-sound/rollover-notification), starting with the "next best action" nudge on Home since it's pure logic + DOM, easiest to verify by careful reading without a display. Added a `gm-nudge` banner above the Daily Quests list in `src/index.html`, styled in `src/styles.css` to match the existing amber-accent/quest-card design language. `renderHomeGamification()` (src/renderer.js) now computes it for free from data it already fetches every render (no new IPC calls): among incomplete daily quests, prioritizes one whose category has an active streak (current_streak>0, picking the highest — most to lose) with a "keep your streak alive" message, falling back to the highest-`base_xp` incomplete quest ("biggest win available") when no streak is at risk; when all daily quests are done it shows a completion message instead of an action; when there are no daily quests at all it hides entirely rather than showing an empty/broken banner. The button reuses the existing `gmCompleteQuest(id,true,event)` call so completing straight from the nudge gets the same XP-fly/toast/streak/level-up handling as the quest list itself. Confetti/sound on quest complete and the daily quest auto-reset notification at rollover are still open for a future pass. `node --check` passes on all four core files (index.html/styles.css reviewed by inspection, not JS).
- 2026-07-29: fifteenth pass (second today), next slice of "ADHD loop tuning" — confetti/sound on quest complete. Added `confettiBurst(ev)` and `playChime()` (src/renderer.js) alongside the existing `coinBurst`/`flyChip` celebration helpers: confetti reuses the same GSAP-particle pattern already established there (DOM pieces flung from the click position, faded/rotated out via `gsap.fromTo`, new `.confetti-piece` class in src/styles.css), and the chime is a tiny 3-note Web Audio oscillator arpeggio (no bundled asset file, matches the "no npm install unless needed" constraint) reusing one shared `AudioContext`. Both are wired into `gmCompleteQuest`'s success branch (fires only on complete, not uncomplete) and gated by a new persisted `st.fxEnabled` setting (default `true`, added to the `load()`/`save()` key lists) so they can be turned off; added a "Celebration effects" toggle to the Settings page (`src/index.html`, reusing the existing `.toggle`/`.toggle-slider` CSS already used by the notification rows) that previews itself when switched on and is initialized from `st.fxEnabled` in `rSettings()`. Also cleaned up a stale duplicate "ADHD loop tuning" bullet left in the v1.2+ list from before this sub-item was split out into its own tracked line. Daily quest auto-reset notification at rollover is the last open item in this bullet. `node --check` passes on all four core files (index.html/styles.css reviewed by inspection, not JS); not runnable end-to-end in this environment (no display, and Web Audio/GSAP need a live renderer window), so verified by careful reading against the existing coin/XP celebration code paths it mirrors.
- 2026-07-29: sixteenth pass (third today), final slice of "ADHD loop tuning" — daily quest auto-reset notification at rollover, closing out this v1.2+ bullet. The existing notification system (`checkNotifications()`/`initNotifications()` in src/renderer.js) only fires at fixed wall-clock times (bedtime/morning/habit/AI-nudge); there was nothing watching for the app-date itself changing, which is when `db.js` actually resets which daily quests are "done" (the `day_rollover_hour` setting, default 4am local). Added `computeLocalAppDate(rolloverHour)`, a renderer-side mirror of `db.js`'s `getAppDate()` local-time rollover math (deliberately not reusing SQL — this only needs a display-layer date string, not a DB round trip per quest action), and `checkQuestReset()`: fetches `day_rollover_hour` via the existing `settings:get` IPC, computes today's app-date, and compares it against a newly persisted `st.lastAppDate`. On a genuine change (never on first load — guarded by `st.lastAppDate` being non-empty) it fires a `🔄 Daily quests reset` notification (respecting a new `questReset` toggle in `st.notifSettings`, default on, added as its own row — no time picker, since it's event-triggered not clock-triggered — in the Settings notification list) and re-renders whichever of Home/Game is currently active so the quest cards visually flip back to unchecked without waiting for a manual tab switch. Wired into the same `setInterval(...,60000)` cadence `initNotifications()` already uses for the other notification checks, so no new polling loop. Verified by reading: traced `st.lastAppDate` through `load()`/`save()`'s key lists and the `st.notifSettings.questReset` default/migration guard for existing users, confirmed `computeLocalAppDate`'s rollover branch matches `getAppDate()`'s (`now.getHours()<rolloverHour → date-1`) side by side, and confirmed the settings:get IPC handler (`main.js:379`) returns `db.getSetting('day_rollover_hour')` which `migrate()` seeds to `'4'` by default so `parseInt(...)||4` never falls through to a wrong default. Not runnable end-to-end in this environment (no display, Notification API needs a live renderer window); `node --check` passes on all four core files. All three "ADHD loop tuning" items (nudge, confetti/sound, rollover notification) are now done — see the v1.2+ list.
- 2026-07-29: seventeenth pass (fourth today), next v1.2+ item — "AI: feed bills/subscriptions into the prompt; apply parseMD beyond journal." Checked the AI system-prompt builder (`callGroq()`, src/renderer.js:1274) first: subscriptions were already fed in with proactive due-date flagging (line 1280, `DUE IN Nd` when a payment is within 3 days) — that half of the item was already done, and there's no separate "bills" concept in the data model (subscriptions is the only recurring-payment structure), so nothing to add there. The `parseMD` half was real: it's already used for Uni Notes and Dev Notes (each has a click-to-edit display/edit toggle where the display side renders through `parseMD`, escaping HTML first then applying `**bold**`/`*italic*`/headers/bullet lists), but Fitness Notes — the third free-text notes field with the identical shape — was still a bare always-visible textarea with a plain Save button, no markdown rendering. Extended the established pattern to it: added `fitness-notes-display`/`fitness-notes-edit` divs in src/index.html (identical structure/styling to `uni-notes-display`/`uni-notes-edit`), updated `rFitness()` (src/renderer.js) to render `st.fitnessNotes` through `parseMD()` in the display div (with the same `emptyState()` placeholder when empty) and hide the edit div on render, and added `saveFitnessNotes()` mirroring `saveUniNotes()`. `rFitness` was already wired into the tab-render dispatch table (`renders.fitness`), so no navigation wiring needed. Note: neither the old nor new fitness-notes save path calls `sanitizeText()` on write (matching `saveUniNotes`/`saveDevNotes`, which don't either) — this is safe because `parseMD()` HTML-escapes `&`/`<`/`>` before applying any markdown formatting, so raw markup can't reach `innerHTML` unescaped, same guarantee the Uni/Dev notes fields already relied on. `node --check` passes on all four core files (index.html reviewed by inspection, not JS); not runnable end-to-end in this environment (no display).
- 2026-07-30: eighteenth pass, first "installer polish" slice — data export/backup and ADHD loop tuning and the AI/parseMD item are all done, so moved to the next v1.2+ item: "Installer polish: signed feel, first-run onboarding, tray icon, launch-on-startup toggle." Picked launch-on-startup first as the most self-contained sub-part (single well-defined Electron API, no packaging/build-config changes, no new dependencies) per this item's own "keep changes conservative" flag. Added `app:get-login-item-settings`/`app:set-login-item-settings` IPC handlers in main.js wrapping `app.getLoginItemSettings()`/`app.setLoginItemSettings({openAtLogin})` (boolean-typed input validated at the IPC boundary, matching the existing handler style), exposed via a new `electronAPI.getLoginItemSettings()`/`setLoginItemSettings()` bridge in preload.js, and a "Launch on startup" toggle in the Settings page (src/index.html) styled identically to the existing Celebration-effects toggle. `rSettings()` (src/renderer.js) now queries the current OS-level setting on each render to keep the toggle in sync with reality (rather than trusting a locally-cached value, since this is OS state that could change outside the app); `setLaunchOnStartup()` calls the IPC setter, toasts success/failure, and reverts the checkbox visually if the main process reports failure. `app.setLoginItemSettings` is a documented no-op (not a crash) on Linux, so this is safe to leave unguarded for dev/this environment even though the shipped build is Windows-only (package.json `build.win`). Not runnable end-to-end here (no display, and login-item registration needs a real OS session) — verified by reading main.js/preload.js/renderer.js/index.html side by side against the existing `fx-toggle` pattern this mirrors. `node --check` passes on all four core files (index.html reviewed by inspection, not JS). Remaining installer-polish sub-items (signed feel, first-run onboarding, tray icon) are still open for future passes.
- 2026-07-30: nineteenth pass (second today), next installer-polish slice — system tray icon, another single-well-defined-Electron-API sub-item with no packaging/build-config changes needed (the `src/icons/icon-*.png` set used for the PWA manifest already ships inside `files: ["src/**/*", ...]` in package.json, so no new asset had to be added). Added `createTray()` in main.js: builds a 16x16 `nativeImage` from the existing `src/icons/icon-96.png`, creates a `Tray` with a context menu ("Open SychBoard" / "Quit") and a left-click handler that restores/shows/focuses the existing `mainWindow` — deliberately did *not* change the window's close behavior (no intercepting the `close` event to hide-to-tray instead of quitting), since that's a real behavioral change beyond "add a tray icon" and this item's own note says keep installer-polish changes conservative; `window-all-closed` still quits exactly as before. Wired `createTray()` into `app.whenReady()` right after `createWindow()`, and added `tray?.destroy()` to the existing `will-quit` handler alongside the `mcp.stop()` cleanup already there. The whole thing is wrapped in try/catch (mirrors the `checkForUpdates`/IPC-handler error-swallowing convention already used throughout main.js) since `Tray` construction can throw in headless/no-desktop-session environments — relevant here since this sandbox has no display and building it would otherwise be inspection-only anyway. Not runnable end-to-end here (no display, no real OS session for the tray icon to render into); verified by reading the Electron `Tray`/`Menu`/`nativeImage` API surface against main.js's existing patterns (window lifecycle, error handling, IPC-handler style). `node --check` passes on all four core files. Remaining installer-polish sub-items: signed feel, first-run onboarding.
- 2026-07-30: twentieth pass (third today), last self-contained installer-polish slice before "signed feel" (a build/packaging-config item, riskier to touch unattended per this item's own note) — first-run onboarding. Audited the existing onboarding flow (`obNext`/`obFinish`/`activateDanielMode` in src/renderer.js, the multi-step name/focus-area/color wizard) and found it entirely predates the SQLite gamification rebuild: it never mentions quests, XP, levels, streaks, SychCoins, the shop, or badges, so a first-time user (or any existing user updating from before this feature) lands straight on Home with level/streak/coin widgets and a daily-quest list already populated and zero explanation of what any of it means or how it's earned. Rather than editing the existing wizard (a separate, already-complete concern — user identity/section setup), added a one-time explanatory modal that surfaces once on first entering the app proper: `modal-game-intro` in src/index.html (five compact rows, reusing the existing `.si`/`.sl`/`.ss` settings-row classes for zero new CSS, explaining Quests/Levels/Streaks/SychCoins/Badges in one line each) plus `closeGameIntro()` in src/renderer.js which sets a new persisted `st.gameIntroSeen` flag (added to state defaults and both load/save key lists) and closes the modal. Wired into `enterApp()` — the function that runs every time boot hands off into the live app — with a 1.2s delay so it appears after Home's own entrance render rather than competing with it, gated by `if(!st.gameIntroSeen)` so it only ever shows once per install (existing users on an update see it too, since they've never seen it either; that's the intended behavior, not a bug). Verified by reading: confirmed `S.get(k)` in `load()` returns `null`/leaves the `false` default for users with no stored `gameIntroSeen` key, confirmed the modal follows the exact same `openModal`/`closeModal`/no-backdrop-click-to-dismiss convention every other modal in the app already uses, and confirmed the button is the only dismiss path (matching every existing modal, no accidental double-fire risk). Not runnable end-to-end here (no display); `node --check` passes on all four core files (index.html reviewed by inspection, not JS). Remaining installer-polish sub-item: signed feel (build/packaging config — flagged as needing conservative treatment given no display to verify against).
- 2026-07-30: twenty-first pass (fourth today), "signed feel" — the last installer-polish sub-item, approached conservatively per its own flag since there's no way to run electron-builder or inspect a real installer/exe in this environment. Confirmed via `.github/workflows/release.yml` that the actual build job runs on `windows-latest` (native rcedit/signtool, no wine involved), so this change only affects a real Windows build/release — which only happens after a merge to main, out of this routine's scope regardless. Rather than touch `win.signAndEditExecutable` (already `false`; couldn't get authoritative docs on its exact behavior via the sandboxed network to safely flip it, and no code-signing cert exists to sign with anyway) or any build target/file list, made a purely additive metadata change: package.json had no top-level `author` field and no `build.copyright`, so the NSIS installer's Publisher field, Add/Remove Programs entry, and the built .exe's file-properties "Company"/"Copyright" fields would all show blank or a raw appId instead of a real identity — one of the more visible "this looks unsigned/sketchy" signals independent of the actual Authenticode signature (or lack of one). Added `"author": {"name":"Sych0n","email":"danieltcpires@gmail.com"}` (matching the git identity already used for commits on this repo) and `"build.copyright": "Copyright © 2026 Sych0n"`. Both are standard, well-documented electron-builder/npm fields (verified `author` against `app-builder-lib`'s `metadata.ts` on GitHub) that only supply descriptive text — no signing, target, or file-list behavior changes, so no new failure mode for the build. `node --check` passes on all four core files (package.json validated by `require()`ing it and checking the new fields parse correctly, not JS-checkable). This closes every sub-item of installer polish that's safe to do without a real code-signing certificate; actual Authenticode signing (the only remaining way to truly stop the Windows SmartScreen "unknown publisher" warning) needs a paid cert Daniel would have to acquire and wire into CI secrets himself, so it's left out of this routine's scope. With signed-feel done, the only self-contained v1.2+ item left unstarted is YouTube API / Trading 212 live data, which the roadmap already flags as needing real API keys this environment doesn't have.
- 2026-07-31: twenty-second pass, no changes — with every self-contained v1.2+ item now done (data export, ADHD loop tuning ×3, AI/parseMD, installer polish ×4) and only YouTube API/Trading 212 live data left (blocked on real API keys per this roadmap's own scoping note), specifically re-audited the four least-covered recent additions — tray icon (`createTray`/`will-quit` cleanup in main.js), launch-on-startup IPC (`app:get/set-login-item-settings`, boolean-validated), the first-run game-intro modal (`gameIntroSeen` flag through load/save key lists), and fitness-notes markdown rendering (`parseMD` escape-then-format, matching the uni/dev notes pattern) — for anything the original implementation passes missed. Found nothing: all four are internally consistent with their established sibling patterns. `node --check` passes on all four core files. The bugs/v1.1/code-quality/v1.2+ queue remains fully exhausted; no notification sent this run per standing guidance (nothing new to report beyond what's already been said in prior passes).
- 2026-07-31: twenty-third pass, second today — with the SQLite gamification system and its own app-date logic having been the near-exclusive focus of ~20 prior audits, widened scope to territory none of them had reached: the original localStorage-backed habit/sleep tracking, predating the SQLite rebuild entirely. Found a live instance of the same "app-date/real-date mixing" bug class (#4) db.js was fixed for repeatedly (2026-07-23) — just never ported to this older code. `recordHabitHistory()`, `logSleep()`, `clearOldSleepLogs()`, `renderSleepChart()`'s day list, and `rSleep()`'s "today" all computed their date key via `new Date().toISOString().slice(0,10)` (UTC calendar day), while the sibling function that actually decides when to roll a new day over, `maybeResetHabits()`, correctly used local `toDateString()`. Reproduced under `TZ=America/New_York`: a habit ticked at 9pm EST July 30 (already past UTC midnight) got misfiled into `habitHistory["2026-07-31"]` by the old code, while `maybeResetHabits()` — checked side by side — still correctly considered it July 30 and hadn't reset yet; for any non-UTC-0 timezone this both corrupts the 30-day habit heatmap (the true local day's box can show "No data" while its completions get merged into the neighboring day) and can silently overwrite or duplicate a night's sleep log depending on which side of UTC midnight it's logged on. Added a `localDateStr(d)` helper (local-field YYYY-MM-DD, no UTC round-trip — same shape as db.js's `formatLocalDate()`) and swapped it in at all five call sites (`recordHabitHistory`, the AI assistant's `log_sleep` action-tag handler, `logSleep`, `clearOldSleepLogs`, `renderSleepChart`'s day loop, `rSleep`'s today lookup); deliberately left the backup-filename timestamp and the YouTube Analytics 28-day API date-range window alone — cosmetic/external-facing, not local data-integrity issues, so not worth the extra diff. Verified the reproduction above now returns `"2026-07-30"` matching `maybeResetHabits()`'s local day exactly. `node --check` passes on all four core files.
- 2026-07-31: twenty-fourth pass, third today, no changes — with the localStorage/SQLite date-mixing sweep now closed, turned to the one genuinely new, previously-unaudited surface in the repo: today's own `sychboard-mcp: server-enforced permissions, restart/git write-tools` commit (a1580d4), which added four confirm-tier write/action tools (`restart_chuck_bird`, `git_diff`, `git_commit`, `git_push`) an LLM-driven chat assistant can invoke, plus the server-side enforcement replacing client-trusted approval. Read `sychboard-mcp/server.js`, `commit-dialog.ps1`, `permissions.json`, `mcp-client.js`, and the `main.js`/`preload.js`/`renderer.js` bridge end-to-end as a security review (command construction, path/repo scoping, approval-bypass potential), not just a style pass. Confirmed: every `execFile` call uses an argument array (no shell interpolation) so the fixed SSH/git commands can't be injected into; `git_diff`/`git_commit`/`git_push` take `repo` from a closed zod enum over exactly two hardcoded absolute paths, no free-text path ever reaches git; `git_push` refuses `main`/`master` unless the caller names that exact branch AND it matches the actual current branch; the renderer's chat-bubble approval (`askToolApproval`, gated on `meta.mode`) is a UX nicety only — real enforcement is `mcp-client.js`'s `callTool()` (main process, reads `permissions.json` itself, ignores a forged `approved:true` from a compromised renderer for anything not literally `mode:'auto'`) layered under `server.js`'s own `confirmPermission`/`confirmCommitMessage` (native OS dialog spawned by the server process itself, independent of both renderer and MCP client trust), so a malicious/injected tool call can get a human to see an *extra* prompt but never skip the real one. Tool-bubble text (name/description/args) all round-trips through the existing `escAttr()` XSS-escaping helper. No exploitable gap found; nothing changed this pass. `node --check` passes on all four core files (no logic touched).
- 2026-07-31: twenty-fifth pass, fourth today — re-audited bugs/v1.1/v1.2+ once more (all still confirmed done/blocked), then did a fresh line-by-line read of `preload.js` specifically, since prior passes had audited it mainly for bridge-completeness (matching main.js handlers) rather than for dead fields on the exposed objects themselves. Found `electronAPI.platform` and `electronAPI.version` (`process.platform`/`process.env.npm_package_version||'1.0.16'`) were both unused — grepped every `electronAPI.*` and `electronAPI[...]` access across `src/renderer.js` and `src/index.html` and neither field is ever read (the one place that touches `window.electronAPI` without a specific field, `renderer.js:449`, only checks the object's truthiness to toggle a CSS class). `version` was also a latent staleness bug beyond being dead: `npm_package_version` is only populated when a process is launched via `npm run`/`npx`, not when Electron spawns the packaged app directly, so in a real build this always silently falls back to the hardcoded `'1.0.16'` literal — a value nothing enforces staying in sync with `package.json` on future version bumps (the roadmap's own v1.1 step 5 is "bump version, merge → main"). Same dead-code shape as the 2026-07-22 `coins:award`/`streaks:add-freeze`/`profile:update-name` cleanups. Removed both fields from the `electronAPI` bridge in `preload.js`; no other file referenced them, so no call-site changes needed. `node --check` passes on all four core files.
- 2026-08-01: twenty-seventh pass — with the Electron app's bugs/v1.1/v1.2+ queue genuinely exhausted (per ~26 prior passes) and YouTube API/Trading212 still blocked on live keys, widened scope to a subsystem no prior pass had touched: `netlify/` and its edge function, present alongside the Electron app in this same repo. Found a critical, real, currently-live issue (not a game-rebuild code bug, so not "fixed" as a normal roadmap item) — see the new "🚨 CRITICAL" section above for full detail: `inject-env.js` bakes real Groq/YouTube/Trading212 secrets into every HTML response with no auth gate in the repo config, present on `main` since commit `7f8dbf5` (2026-04-21). Deliberately did not attempt a code fix this pass (needs Daniel's call on rotation + site-privacy status + which architecture fix), flagged via notification instead. No changes to any of the four core Electron files this pass; `node --check` still passes on all four (unmodified).
- 2026-08-01: twenty-sixth pass — with db.js re-read in full this session (traced `completeQuest`/`uncompleteQuest`'s streak/coin/sweep-bonus symmetry again, found nothing new), delegated a fresh sweep of the least-audited corners to a subagent rather than re-covering db.js/main.js/renderer.js's already-exhausted hot paths. It found a real, previously-missed stored/reflected-XSS gap in `renderYTVideos()` (src/renderer.js) — the 8-pass stored-injection sweep from 2026-07-26/27/28 covered every *locally authored* text field (habits/goals/todos/notes/events/shifts/exam-date/username) but missed that YouTube video titles fetched from the real YouTube Data API (`v.snippet.title` in `fetchYTData`, cached into persisted `st.yt.apiVideos`) were interpolated straight into an `innerHTML` template with no escaping. Unlike the earlier fixes, this is externally-controlled data: the channel ID is user-configurable (Settings) and doesn't have to be the user's own channel, so any video title containing markup (e.g. an `<img onerror=...>`) from *any* channel the user points the dashboard at would execute as real DOM/script in the renderer — able to read `st.apiKeys` (Groq/T212/YouTube keys, in memory) or drive the exposed `electronAPI` bridge, given the page's permissive CSP. Verified by reading the exact data flow (API response → `fetchYTData` → `st.yt.apiVideos`/`ytApiCache` → `renderYTVideos`'s `innerHTML` at the render call) and confirming neither `sanitizeText()` nor `escAttr()` (both already used for this exact interpolation-into-innerHTML shape elsewhere, e.g. the AI tool-call bubbles at renderer.js:1234/1236/1249) were applied. Fixed by wrapping `v.title` and `v.published` in `escAttr()` at the `renderYTVideos` template (renderer.js). `node --check` passes on all four core files. Not runnable end-to-end here (no display, no live YouTube API key); verified by reading the render path and confirming `escAttr`'s escaping matches the pattern already proven safe at the other call sites.
- 2026-08-01: twenty-eighth pass — with bugs/v1.1/v1.2+ self-contained items and the prior 27 passes' code-quality sweeps all still holding, delegated a fresh line-by-line read of main.js's least-scrutinized IPC handlers (Groq chat, Trading212, YouTube fetch/OAuth/analytics) to a subagent rather than re-covering already-exhausted ground. It found a real data-corruption bug in all 4 live HTTPS response handlers (`youtube-fetch`, the OAuth token-exchange call inside `youtube-oauth-start`, `youtube-oauth-refresh`, `youtube-analytics-fetch`): each accumulated the response body via `raw += c` where `c` is a `Buffer`, implicitly calling `c.toString('utf8')` on every chunk independently — since Node's TCP chunk boundaries aren't guaranteed to land on UTF-8 character boundaries, a multi-byte character (emoji, non-Latin script) split across two chunks gets each half decoded separately and silently replaced with U+FFFD, corrupting the string before `JSON.parse` runs. This is realistic, not hypothetical: YouTube titles/descriptions routinely contain emoji, and multi-item API responses are large enough to span multiple ~1460-byte TCP segments — the failure is intermittent (depends on network chunking) and would masquerade as the existing generic "Parse error"/"API returned invalid data" paths, making it effectively unreproducible/undiagnosable from the logged `raw.slice(...)` since the corruption already happened. Fixed by calling `res.setEncoding('utf8')` right after obtaining each response object (before the `'data'` listener) at all 4 live call sites — Node then buffers incomplete multi-byte sequences internally between chunks instead of decoding each one in isolation, making the existing `raw += c` accumulation correct with no other logic changes. Left the identical pattern in the disabled/commented-out `t212-fetch` body alone (dead code, not executed while the integration is off). `node --check` passes on all four core files.
- 2026-08-01: twenty-ninth pass — checked a corner none of the prior 28 passes had looked at: `createWindow()`'s `setWindowOpenHandler` in main.js (`mainWindow.webContents.setWindowOpenHandler(({url}) => { shell.openExternal(url); return {action:'deny'} })`) called `shell.openExternal` on *any* URL a `window.open()` call could pass, with no scheme check. Electron's own security guidance flags this as a real risk when combined with any path that lets untrusted content reach the renderer: given this app's long history of stored-`innerHTML`-injection bugs found and fixed across ~8 separate passes (2026-07-26 through 2026-07-31), a residual or future gap plus `window.open('file://...')` (or another non-http(s) scheme) could hand `shell.openExternal` a local file/program to launch via the OS's default handler — arbitrary-ish local code execution, not just a cosmetic DOM issue. Confirmed no code in the app currently calls `window.open`/`target=_blank` itself (grepped `main.js`/`src/*.js`/`src/index.html`), so this was pure defense-in-depth, not a currently-reachable bug; the OAuth flow's own `shell.openExternal(authUrl)` call (main.js, inside `youtube-oauth-start`) was separately confirmed safe since `authUrl` is built from a hardcoded `https://accounts.google.com/...` host, not attacker input. Fixed by parsing the URL and only calling `shell.openExternal` when the scheme is `http:`/`https:`, logging and denying anything else (matching the existing `console.error` convention used throughout main.js's other handlers). `node --check` passes on all four core files.
- 2026-08-02: thirtieth pass — with bugs/v1.1/v1.2+-self-contained/code-quality all still holding (re-verified main.js, mcp-client.js, sychboard-mcp/server.js, commit-dialog.ps1, permissions.json, and the renderer's AI-action/MCP-tool call paths end-to-end this pass, no new logic bugs), found a real packaging gap in the one place none of the ~29 prior passes had checked: `package.json`'s `build.files` (electron-builder) and `.github/workflows/release.yml`. The `sychboard-mcp/` subsystem (`server.js`, `permissions.json`, `commit-dialog.ps1` — the whole "AI OS" tool layer: `get_chuck_bird_status`/`get_system_status`/`list_recent_project_files`/`restart_chuck_bird`/`git_diff`/`git_commit`/`git_push`, wired into main.js/preload.js/renderer.js since `a1580d4`) was **never included in the packaged app at all**: `build.files` explicitly overrides electron-builder's file list with `["main.js","preload.js","src/**/*","assets/**/*","node_modules/**/*"]` — no entry matches `sychboard-mcp/`, a sibling directory at the repo root — so `mcp-client.js`'s `ensureStarted()` (`if (!fs.existsSync(SERVER_JS)) throw ...`) would fail immediately in every real installed build, even though the feature works perfectly via `npm start` in dev (where electron-builder's `files` filter never applies). Digging further, even fixing the `files` glob alone wasn't enough: `sychboard-mcp/node_modules` (holding `@modelcontextprotocol/sdk`/`zod`) is git-ignored (repo-wide `node_modules/` rule) and CI's `build` job (`.github/workflows/release.yml`) only ever runs `npm ci` at the repo root — it never installs `sychboard-mcp`'s own dependencies — so the Windows build runner would never have `sychboard-mcp/node_modules` on disk to bundle in the first place, meaning `mcp-client.js`'s *second* existence check (`sychboard-mcp has no node_modules`) would also have failed post-fix. Net effect pre-fix: the entire MCP/"AI OS" tool layer — despite ~5 dedicated security-review passes praising its permission-gating design — has been silently dead in every real user-facing build since it was added, a dev-only feature masquerading as shipped. Fixed both halves: added `"sychboard-mcp/**/*"` to `build.files` in package.json (a single recursive glob covers the source files and, once installed, its `node_modules`), and added an `npm ci` step scoped to `working-directory: sychboard-mcp` in the `build` job of `release.yml`, right before `npm run publish`. Confirmed neither of `sychboard-mcp`'s two dependencies (`@modelcontextprotocol/sdk`, `zod`) ship any native `.node` binaries (pure JS), so plain asar packaging is sufficient — no `asarUnpack` needed. Not runnable end-to-end here (no Windows runner, no `npm ci`/electron-builder in this environment) — verified by reading electron-builder's `files`-override semantics (an explicit array replaces the default file set rather than adding to it) against the actual directory layout, and by reading the CI job step-by-step against `mcp-client.js`'s two `existsSync` guards. `node --check` passes on all four core files (unchanged this pass); `package.json` validated via `JSON.parse`; `release.yml` validated via a YAML parse.
- 2026-08-02: thirty-first pass — with bugs/v1.1/v1.2+-self-contained/code-quality still holding, re-swept main.js's remaining IPC handlers not previously called out by name (`app:get/set-login-item-settings`, `restart-and-install`, `env:get-keys`, `settings:get/set`, `data:export-game/import-game`, `mcp:list-tools/call-tool`), `db.js`'s `setSetting` (uses `String(value)`, safe against any input shape), and re-verified `sychboard-mcp/server.js`'s `gitPush`/`commit-dialog.ps1` trust chain (branch-name self-matching is comment-level guidance only, but the real gate is `permissions.json`'s `mode:"confirm"` on `git_push`/`git_commit`, which forces a human-visible native dialog server-side regardless of what the calling LLM claims) — no new logic bugs found in any of these. Also checked two previously-unaudited root files: `scripts/gen-icons.js` (dev-only PNG icon generator, not shipped/invoked at runtime, nothing to fix) and `diag_main.js`, a leftover Electron-module debug probe from the original `7f8dbf5` commit that dumps `typeof electron`/`ipcMain` to console and exits — confirmed via grep it's never required/referenced by `main.js`, any script, or `package.json`'s `build.files`, so it's pure dead cruft at the repo root, same shape as the `coins:award`/`profile:update-name`/`electronAPI.platform`+`version` cleanups from earlier passes. Deleted it. `node --check` passes on all four core files (unaffected — it was never imported by any of them).
- 2026-08-02: thirty-second pass — follow-up on the "package sychboard-mcp into the build" fix from earlier today (6d60c90): that fix got the whole `sychboard-mcp/` directory shipping inside the asar archive, but missed that one file inside it is read by something other than Node. `confirmCommitMessage()` (sychboard-mcp/server.js) spawns `powershell.exe -File <path>` pointing at `commit-dialog.ps1` via `path.join(__dirname, ...)` — powershell.exe is a real external OS process with zero knowledge of Electron's asar virtual filesystem, so once `commit-dialog.ps1` ships inside `app.asar` (as it now does), `__dirname` resolves to a path like `...\resources\app.asar\sychboard-mcp`, which isn't a real path any non-Electron process can open; `-File` would fail immediately in every packaged build. This is the same "works in dev, silently dead when packaged" shape as this morning's fix, one layer deeper — the directory ships, but this specific script inside it becomes unreadable by exactly the process that needs it. Only `commit-dialog.ps1` is affected: `confirmPermission`'s native yes/no dialog uses an inline `-Command` script string (no file path involved), so it was never at risk. Fixed by adding `sychboard-mcp/commit-dialog.ps1` to a new `build.asarUnpack` entry in package.json (electron-builder copies it to `app.asar.unpacked/...`, a real path alongside the archive) and computing a `COMMIT_DIALOG_PS1` constant in server.js that maps `__dirname` onto the `.unpacked` mirror when the string contains `app.asar` (a no-op in dev, where `sychboard-mcp` runs from a real directory with no `.asar` in its path at all). Not runnable end-to-end here (no Windows runner, no electron-builder packaging step in this environment) — verified by reading electron-builder's documented `asarUnpack` semantics (unpacked files land at `<archive>.unpacked/<relative path>`, but `__dirname`/`require.resolve` paths for code still living inside the asar are untouched, which is why the path needs remapping in application code rather than being automatic) against the actual spawn call. `node --check` passes on all four core files plus `sychboard-mcp/server.js` and `mcp-client.js`; `package.json` validated via `JSON.parse`.
- 2026-08-02: thirty-third pass — with main.js/preload.js/db.js re-read in full this pass (no new gaps: `env:get-keys` is dev-only and safely no-ops when `.env` is absent from a packaged build; `data:import-game` correctly defers all shape validation to `db.importGameData`'s own checks) and the gamification-heavy 2/3 of renderer.js already covered by ~10 prior sweeps, delegated a fresh read of the less-scrutinized third — finance/shifts, Trading212, journal, AI chat action-tag execution, and index.html's inline scripts — to a subagent. It found a real money-math bug in `rShifts()` (src/renderer.js): the Finance tab's "This month" shift-earnings total filtered `new Date(s.date+' '+now.getFullYear()) >= ms` (start of current month) with no upper bound, so (a) a shift dated later in the calendar than today but logged in advance for a future month (e.g. a Christmas shift logged in August) was wrongly counted into the current month's total, and (b) a shift from December of a prior year, re-parsed against the current year when viewed early the following year, was also wrongly included. Verified the reported repro by reading the code directly (`src/renderer.js:1828-1836`) before fixing: with shifts dated "25 Dec"/"1 Aug"/"15 Jan" viewed as of Aug 2026, the old filter summed the Dec-dated shift into "this month" too, overstating the total. Fixed by adding the missing upper bound (`me`, start of next month) so the filter is a true `[start of month, start of next month)` window. `node --check` passes on all four core files.
- 2026-08-03: thirty-fourth pass — delegated a fresh audit of genuinely unexplored territory (index.html inline handlers, styles.css, adjacent renderer.js edge cases) to a subagent since 33 prior passes had exhausted the usual hot paths. It found a real gap in Settings → "Clear all data" (`src/index.html`): the button's label, description, and confirm-dialog copy all promise wiping "everything," but the `onclick` handler only ever called `localStorage.clear()` — it never touched the SQLite gamification DB (`src/db.js`: profile/XP/level/SychCoins/streaks/quest history/badge unlocks), so a user leveled up with badges and streaks who clicked "Clear data" for a genuine fresh start would still see their old level/coins/streak on the next launch, silently contradicting the UI's own "permanently deletes all your data" claim — the identical gap the 2026-07-28 data-backup fix closed for *export*, just never carried over to this destructive sibling action. Added `db.clearGameData()` (src/db.js: one transaction — deletes quest_completions/xp_log/badge_unlocks/streaks/settings, resets `profile` to defaults, reseeds one zeroed `streaks` row per category and the three settings defaults, mirroring `importGameData`'s reset shape), a `data:clear-game` IPC handler (main.js) and `data.clearGame` preload bridge, and wired the index.html button to await it before clearing localStorage and reloading. Verified against a real SQLite DB (`node:sqlite` shim): completed a quest and attempted a shop purchase to build up state, called `clearGameData()`, confirmed profile/coins/streaks all reset to fresh-install defaults immediately after (not just after a subsequent action), the quest/badge *catalog* survived untouched (27 quests, badges list intact with `unlocked:0`), recent activity was empty, and a normal `completeQuest` call right after the clear succeeded with no stale unique-constraint conflicts (confirming the reseeded streaks/quest_completions state is genuinely fresh, not just zeroed-looking). `node --check` passes on all four core files.
- 2026-08-03: thirty-fifth pass (second today) — the ~9-pass stored-injection sweep (2026-07-26 through 08-01) had covered every free-text field rendered as innerHTML *text content*, but none of those passes checked for the sibling attribute-breakout shape: user text reflected into an HTML *attribute* (`value="${...}"`) inside an innerHTML template. Grepped every `value="${` interpolation in `src/renderer.js` for this specific pattern and found one real instance: the Finance tab's holiday name/date fields. `updHol()` wrote `document.getElementById('hn-'+i).value`/`'hd-'+i` straight into `st.holidays[i].name`/`.date` with no escaping or length cap, and `rFinance()` reflects `h.name`/`h.date` unescaped into `value="${h.name}"`/`value="${h.date}"` inside `di.innerHTML`. A holiday name like `x" onfocus="alert(1)" autofocus="` or `x"><img src=x onerror=alert(1)>` would break out of the input's `value` attribute (or the tag entirely) and execute as real markup/script on the next Finance render — same severity as the eight `innerHTML`-text-content injection bugs fixed in the prior sweep, just missed because those passes only grepped for text-content interpolation, not attribute interpolation. The other five `value="${...}"` sites in renderer.js are all either API keys (not attacker-reachable free text in the same sense) or `st.notifSettings` time-picker values (constrained `<input type="time">` values, not free text). Fixed by wrapping both fields in `sanitizeText()` at the write site in `updHol()` (40-char cap for name, 30 for date, matching the field's short display use), the same escape-on-write convention already used for every other stored-injection fix in this codebase — including the one place this field's escaped form round-trips through `.textContent` (`ti.textContent=h.name+...`) rather than `innerHTML`, which is the same accepted minor-cosmetic tradeoff (literal `&amp;` instead of decoded `&` for names containing punctuation) that `st.userName` already lives with today across its own mixed `textContent`/`innerHTML` render sites. `node --check` passes on all four core files.
- 2026-08-03: thirty-sixth pass (third today) — with bugs/v1.1/v1.2+-self-contained/code-quality still holding after 35 prior passes, checked `createWindow()` (main.js) against Electron's own hardening checklist since the 2026-08-01 `setWindowOpenHandler` fix had only closed the `window.open()` half of it. Found the top-level-navigation half was still open: there was no `will-navigate` listener on `mainWindow.webContents`, so any top-level navigation away from `src/index.html` (a link click, a redirect, or a future stored-injection gap slipping past the ~9-pass sanitization sweep) would sail through unchallenged and replace the entire app UI with arbitrary remote or `file://` content inside this same privileged (`contextIsolation:true`/`nodeIntegration:false`, but still preload-attached) window — defense-in-depth, not a currently-reachable bug (grepped `src/*.js`/`index.html`, no code triggers a top-level navigation today), same posture as the window-open fix. Added a `will-navigate` handler mirroring that fix's shape exactly: navigation to the app's own `index.html` (compared via `pathToFileURL`) is allowed through, `http(s)` targets are handed to the OS browser via `shell.openExternal` instead of loading in-window, everything else (`file://`, `javascript:`, etc.) is blocked and logged. `node --check` passes on all four core files.
- 2026-08-03: thirty-seventh pass (fourth today) — with bugs/v1.1/v1.2+/code-quality all still holding per 36 prior passes, delegated a fresh read of the most recently-touched area (the three 2026-08-02 packaging fixes for `sychboard-mcp/`) to a subagent rather than re-covering already-exhausted hot paths, specifically checking whether those fixes' own `build.files` edit had introduced a gap next to itself. It found a real, severe one: `package.json`'s `build.files` override (`["main.js","preload.js","src/**/*","assets/**/*","sychboard-mcp/**/*","node_modules/**/*"]`) never included `mcp-client.js` — a repo-root file `main.js:7` (`const mcp = require('./mcp-client')`) requires unconditionally at module load time, before `app.whenReady()` and before the `uncaughtException`/`unhandledRejection` handlers even get a chance to run (a synchronous `MODULE_NOT_FOUND` in the entry script's own top-level `require` crashes the process immediately, it isn't a catchable async error). Since `build.files` is an explicit override (electron-builder replaces the default file set rather than adding to it) and no `extraResources`/`extraFiles` compensates, `mcp-client.js` was missing from every packaged build — worse than the 2026-08-02 fixes' own bug (which only broke the MCP feature), this would have crashed the entire app on launch with `Cannot find module './mcp-client'`, the same "works in dev via `npm start`, silently dead/broken when packaged" shape as those three fixes, just one file over. Independently confirmed the `require` call and the `build.files` array by reading both files directly before fixing. Fixed by adding `"mcp-client.js"` to `build.files`. The subagent's audit also separately confirmed `sychboard-mcp/package-lock.json` matches `package.json` exactly (`lockfileVersion:3`, deps in sync), the CI `npm ci` step for `sychboard-mcp` (`.github/workflows/release.yml`) is correctly ordered/scoped ahead of `npm run publish`, and `asarUnpack`'s `commit-dialog.ps1` path is correct — no other packaging gaps found. `node --check` passes on all four core files; `package.json` validated via `JSON.parse`.
- 2026-08-04: thirty-eighth pass — delegated a fresh research-only sweep to a subagent (bugs/v1.1/v1.2+/code-quality all still holding per 37 prior passes) targeting corners with lighter coverage. It found a real bug in `getStreaks()` (src/db.js): the per-category `current_streak` values it returns are the raw, persisted `streaks.current_streak` column, which is only ever updated inside `completeQuest`/`uncompleteQuest` — i.e. only when that category is actually touched — so unlike the global engagement streak just below it in the same function (fixed 2026-07-23 to check liveness against `getAppDate()` on every read), a category's displayed streak never decays and stays stuck at its last value indefinitely once the user stops touching that category, even after weeks of inactivity. This directly misleads two consumers: the Game tab's streak cards (`src/renderer.js:988`, `c.current_streak`) keep showing "🔥 20d" long after it's actually dead, and the "next best action" nudge (`src/renderer.js:826-834`, added 2026-07-29) uses `catStreak[key]>0` to prioritize "protect your streak" messaging for a category whose streak has nothing left to protect. Fixed by adding a display-only staleness check in `getStreaks()`: for each category row, if `last_completion_date` is neither today's app-date nor yesterday's (the same "1-day gap alive, 2+ broken" threshold already used for the global streak and in `completeQuest`), report `current_streak:0` without touching the stored value — `completeQuest`/`uncompleteQuest` query the `streaks` table directly, not through `getStreaks()`, so they're unaffected and still compute the real transition (including soft-reset) from the true persisted value on the next actual completion. Verified against a real SQLite DB (`node:sqlite` shim): a fresh completion displays `current_streak:1`; manually aging a category's row to 20 days stale with `current_streak:20` persisted now correctly displays `0` while the raw DB row stays at `20` untouched; a 1-day-old (yesterday) completion still correctly displays as alive. `node --check` passes on all four core files.
- 2026-08-04: thirty-ninth pass (second today) — checked the Pomodoro timer (`src/renderer.js`), an area none of the prior 38 passes had touched. Found a real dead-end in `togglePom()`: when a session finishes naturally, `pomL` is left at 0 with no reset path other than the Break/Focus mode-toggle button — clicking "Start" again (the only other control) flips the UI into a "Focusing.../Resting..." state that pulses forever at `00:00`, since the interval's `if(pomL>0)` guard never lets it decrement or fire the completion notification again. Fixed by resetting `pomL` to the current mode's full duration when Start is pressed with `pomL<=0`, mirroring the same reset expression already used correctly in `togglePomMode()`/`savePomSettings()`. `node --check` passes on all four core files.
- 2026-08-04: fortieth pass (third today) — delegated a fresh research sweep to a subagent targeting corners none of the prior 39 passes had touched. It found a real re-entry bug in `enterApp()` (`src/renderer.js`): no idempotency guard, and the `#boot-launch` button (`src/index.html`) stays clickable with no disabled state for up to 900ms after the first click, plus `enterAppAndGo()` (boot-AI chat auto-enter) is a second independent call path — so a double-click or overlapping click-during-auto-enter calls `enterApp()` twice, and `initNotifications()` (called via `setTimeout` from `enterApp()`) unconditionally stacks a second set of `setInterval(checkQuestReset,...)`/`setInterval(checkNotifications,...)` timers with nothing clearing the first pair, unlike the sibling `startLiveStatsUpdates()` which already guards via `liveStatsInterval`. Traced a real consequence, not just wasted CPU: `checkQuestReset()` is `async` and awaits an IPC settings read before comparing/writing `st.lastAppDate`, so two duplicated intervals ticking near the same minute can both pass the stale read before either writes back, both independently detect the day rollover, and both fire a real duplicate OS notification (`'🔄 Daily quests reset'`) plus duplicate re-renders. Fixed with a simple `_appEntered` guard flag at the top of `enterApp()`, mirroring the codebase's existing in-flight-guard pattern (e.g. `questInFlight`); confirmed via grep the only two call sites (`index.html`'s button, `enterAppAndGo()`) are both one-time boot-to-app transitions with no legitimate re-entry flow, so the guard can't block real usage. `node --check` passes on all four core files.
- 2026-08-04: forty-first pass (fourth today) — delegated a fresh research sweep to a subagent (bugs/v1.1/v1.2+/code-quality all still holding per 40 prior passes), targeting Journal, Trading212, goals/todos, styles.css, and Settings toggles as less-audited corners; all came back clean except one real date-math bug it found in the AI assistant's system-prompt builder, `callGroq()` (src/renderer.js:1309) — the proactive subscription-due-date reminder (added 2026-07-29, verified "already done" that day but never checked for arithmetic correctness) computed days-until-due as `(s.date-now.getDate()+31)%31`, hardcoding a 31-day month. For the 5 shorter months (30-day months, and Feb) whenever a subscription's billing day has already passed in the current month — the common case for most of any given month — the wraparound overcounts the true days-until-next-due by 1-3 days, a systematic false-negative bias (never overstates urgency, always understates it). Concretely: on Feb 27 with a subscription due day 2, the real next due date is Mar 2 (3 days away, should trigger "DUE IN 3d"), but the old formula returned 6, silently suppressing the warning — defeating the point of the proactive-reminder feature. Fixed by replacing the fixed-modulus arithmetic with real `Date` arithmetic (construct today-at-midnight and this-month's-due-date, roll to next month if already passed, diff in days) so JS's own calendar normalizes variable month lengths. Verified against 4 cases with a standalone reproduction: Feb27→day2 now correctly returns 3 (old formula gave 6), same-day-due returns 0, Jan31→day1 (month+year rollover) returns 1, Apr29→day5 (30-day month) returns 6 — all correct. `node --check` passes on all four core files.
- 2026-08-05: forty-second pass — delegated a fresh research sweep to a subagent (bugs/v1.1/v1.2+/code-quality all still holding per 41 prior passes), targeting corners not yet covered. It found the same UTF-8 chunk-boundary corruption bug class already fixed 2026-08-01 in main.js's four HTTPS response handlers, but never ported to its sibling IPC channel: `mcp-client.js`'s stdout reader (`ensureStarted()`) accumulated the sychboard-mcp child process's stdout via `buf += d.toString()` on raw `Buffer` chunks with no `setEncoding('utf8')` call, so a multi-byte UTF-8 character split across two chunk boundaries gets each half replaced with U+FFFD before the newline-delimited JSON-RPC line is parsed — silently corrupting any tool response containing non-ASCII text near a chunk boundary (e.g. `™`/`®` in a real CPU model string, an emoji in a git diff/commit message, a unicode filename), either dropping the response entirely (misleading "MCP {method} timed out" after the full 45s timeout despite the server having responded) or corrupting a string field with replacement characters. Fixed by adding `child.stdout.setEncoding('utf8')` right after the child's `exit`/`error` handlers are wired up, matching the exact fix already applied at the four main.js call sites. `node --check` passes on all four core files plus mcp-client.js.
- 2026-08-05: forty-third pass (second today) — delegated a fresh research sweep to a subagent (bugs/v1.1/v1.2+/code-quality all still holding per 42 prior passes), targeting journal, Trading212, badge thresholds, notification scheduling, sleep stats, and goals/todos math as less-audited corners. It found a real bug in the legacy localStorage-backed habits feature: `maybeResetHabits()` (src/renderer.js) — the code that flips every habit checkbox back to unchecked on a new local calendar day — is only ever called once, from `enterApp()` at boot. Unlike the SQLite quest system, which got a dedicated `checkQuestReset()` polled every 60s specifically so a long-running session catches the day rollover (2026-07-29/2026-08-04 passes), habits had no equivalent poll. Concrete failure: with the app now resident via its system tray icon (2026-07-30), a user who completes habits before midnight and leaves the app open sees yesterday's habits still shown as done the next day across the Habits tab, Home stat, and heatmap — until a full relaunch — and toggling any habit in that stale state writes today's heatmap entry with the wrong (still-checked) completion count via `recordHabitHistory()`. Fixed by mirroring the quest-reset pattern: `maybeResetHabits()` now returns whether a reset actually happened, a new `checkHabitReset()` wrapper re-renders the Habits tab (if active) or Home gamification panel when it does, and both are polled from the same `setInterval(...,60000)` loop as `checkQuestReset()`/`checkNotifications()` in `initNotifications()` — no new polling loop, same cadence. Not runnable end-to-end here (no display); verified by reading the call graph (confirmed via grep `maybeResetHabits` had exactly one call site before this fix) and comparing the new poll against `checkQuestReset()`'s already-proven shape. `node --check` passes on all four core files.
- 2026-08-05: forty-fourth pass (third today) — delegated a fresh research sweep to a subagent, targeting goals/todos, badge thresholds, notification-time parsing, journal, Trading212, styles.css, and localStorage save/load key-list completeness as less-audited corners. It found a real bug in the YouTube tab's "Weekly checklist" card (`src/renderer.js`, `rYT()`): the 6-item `YT_CHECKS` list backed by `st.yt.weekChecks` (persisted via localStorage, part of the `yt` key already in `load()`/`save()`) only ever reset when the array was missing or the wrong length — no date/week comparison anywhere, unlike every other recurring feature (daily quests, weekly quests in db.js's `isoWeekRange`, habits as of the pass above). Concrete failure: a box ticked on a Saturday stayed checked forever, since nothing ever set it back to unchecked on a new week — the checklist silently degraded from "what have I done this week" into "what have I ever done at least once," the opposite of its own label and purpose. Fixed by adding `mondayOfWeek(d)` (mirrors db.js's Mon-Sun `isoWeekRange` Monday calculation, next to the existing `localDateStr` helper) and a persisted `st.yt.weekChecksWeekStart`; `rYT()` now resets `weekChecks` to all-unchecked and saves whenever the computed current Monday differs from the stored one, checked on every render of the YouTube tab (no new polling loop needed — this tab isn't typically left open across a week boundary the way the always-visible Habits/Home surfaces are). One accepted side effect: existing users' in-progress checklist state resets once on first load after this update, since there's no way to retroactively know which week their prior checks belonged to — same tradeoff already accepted for other first-run-style migrations in this codebase. `node --check` passes on all four core files.
- 2026-08-05: forty-fifth pass (fourth today) — delegated a fresh research sweep to a subagent (bugs/v1.1/v1.2+/code-quality all still holding per 44 prior passes), targeting corners not yet covered. It found a real bug in the epic/milestone quest system (`src/db.js`): the `quests` table's `repeatable` column defaults to 1, and the v2 seed `INSERT`s for all 5 epic quests (`h_milestone`, `p_theory_test`, `p_ship_release`, `c_channel_milestone`, `f_milestone` — one-time achievements like "Pass theory test" and "Hit a savings milestone") never set it, so they silently inherited `repeatable=1`. `listQuests()`'s epic-visibility filter (`repeatable=1 OR NOT EXISTS(...completion...)`) was written to hide a non-repeatable epic quest forever after its first completion, but with every real epic quest defaulting to repeatable, that clause always short-circuits true and never fires — so all 5 milestone quests, worth 100-150 XP each, were fully re-completable once per day forever through completely ordinary use of the Game tab's Milestones section, undermining the level/streak-bonus/coin economy the bugs-list fixes were built around (up to ~470 bonus XP/day, on top of the normal daily/weekly allowance, just from re-clicking the same five cards). Fixed both the data and the enforcement: added migration v5 (`UPDATE quests SET repeatable=0 WHERE frequency='epic'`) to retroactively correct existing rows, and added an explicit once-ever guard in `completeQuest()` for `repeatable=0` epic quests (mirroring the existing weekly-quest guard) so the rule is enforced server-side rather than relying solely on `listQuests()`'s display filter — the same "don't trust the UI as the only gate" posture behind the 2026-07-28 shop-pricing fix. Verified against a real SQLite DB (`node:sqlite` shim): a fresh DB seeds epic quests with `repeatable=0`; completing one awards XP once and the quest is immediately absent from a follow-up `listQuests()` call; calling `completeQuest()` on it again (simulating a direct IPC call bypassing the UI) now throws `'Epic quest already completed'` instead of granting XP again; a sibling daily quest is unaffected (`repeatable` still defaults to 1, completable next day as normal). `node --check` passes on all four core files.
- 2026-08-06: forty-sixth pass — delegated a fresh research sweep to a subagent (bugs/v1.1/v1.2+/code-quality all still holding per 45 prior passes), targeting Journal, Trading212, goals/todos math, badge thresholds, notification time parsing, sleep stats, and settings bounds validation as less-audited corners. It found a real gap in `day_rollover_hour`: `settings:set` (main.js) only checked the *key* was a non-empty string, never validating the *value*, and `getAppDate()`/the sweep-bonus dedup query/`getXpHistory()` (all three in src/db.js) each read the stored value with a bare `parseInt(...)` and no bounds check against `now.getHours()`'s 0-23 range. A stored value of 24+ makes `getAppDate()`'s `now.getHours() < rolloverHour` permanently true, freezing the app-wide "what day is it" concept one calendar day in the past forever (poisoning quest app-dates, streak continuity, the weekly-quest gate, and XP history bucketing) — not reachable through the current UI (no rollover-hour settings control exists yet), but reachable via a direct IPC call or a hand-edited/corrupted backup restored through the existing `importGameData` path, same threat model as the 2026-07-22/07-28 shop-pricing IPC gaps. Fixed by adding a shared `getRolloverHour()` helper in db.js (clamps to 0-23, defaulting to 4 on any out-of-range or non-numeric stored value) used at all three read sites, plus a boundary check in `settings:set` rejecting any `day_rollover_hour` value that isn't an integer 0-23 before it ever reaches the DB. Verified the clamp and the IPC-boundary check against the exact edge cases (24, -1, NaN/non-numeric, 23, 0) with standalone reproductions — both reject out-of-range/non-integer values and accept the valid boundary values. `node --check` passes on all four core files.
- 2026-08-06: forty-seventh pass (second today) — follow-up on this morning's `day_rollover_hour` bounds fix, which patched the three read sites in db.js plus main.js's `settings:set` IPC boundary but missed a fourth, independent read site: `checkQuestReset()` (src/renderer.js:2460), a hand-rolled renderer-side mirror of `db.js`'s `getRolloverHour()` used to detect the day-rollover boundary for the "🔄 Daily quests reset" notification and Home/Game re-render. It read the setting as `parseInt(...)||4`, which has two bugs `getRolloverHour()`'s proper clamp doesn't: (1) `||4` treats a legitimately-stored `0` (midnight rollover, valid per `settings:set`'s own `>=0` check) as falsy and silently coerces it to 4, so with that setting the renderer's reset-detection stays wrong for a 4-hour window every day even though `getAppDate()` already rolled over at midnight; (2) no upper-bound clamp, so an out-of-range value from a corrupted/hand-edited backup (`importGameData` writes settings rows verbatim, no bounds check) makes `computeLocalAppDate`'s `now.getHours()<rh` permanently true, permanently offsetting the renderer's notion of "today" by a day versus what `getAppDate()` actually reports. Found via a subagent research sweep targeting exactly this "other settings reads" angle after the morning fix. Fixed by replacing the `||4` fallback with the same `Number.isInteger(raw)&&raw>=0&&raw<=23?raw:4` clamp `getRolloverHour()` uses. `node --check` passes on all four core files.
- 2026-08-06: forty-eighth pass (third today) — delegated a fresh research sweep to a subagent, targeting journal, Trading212, goals/todos, badge thresholds, notification-time parsing, sleep stats, AI prompt construction, parseMD, and backup import/export as less-audited corners. It found a real falsy-zero bug in `updateDev()` (src/renderer.js, Dev/Side-Project tab): `if(m)st.dev.members=m` after `parseInt(...)` silently drops a legitimate "set members to 0" entry (e.g. resetting a project's member count) because `parseInt('0')` is falsy, while the `status` field and the "Project updated" success toast both still fire unconditionally — a false-positive success with the stat silently unchanged. Same bug class already fixed elsewhere (`checkQuestReset`'s `||4`, 2026-08-06) and already guarded against in the sibling `updateBal()` (Finance tab), which checks the raw trimmed input rather than the parsed value's truthiness. Fixed `updateDev()` to mirror that pattern: treat a blank field as "leave unchanged" and any valid non-negative integer (including `0`) as a real update. `node --check` passes on all four core files.
- 2026-08-06: forty-ninth pass (fourth today) — delegated a fresh research sweep to a subagent targeting Journal, Trading212, goals/todos, sleep stats, badge thresholds, and notification-time parsing (Trading212 turned out to be dead code — `t212-fetch` is hardcoded to return an error — and Journal is XSS-safe via `parseMD`'s escaping, so both came back clean). It found a real bug in `sleepConsistency()` (src/renderer.js, Sleep tab): to compute the standard deviation of recent bedtimes, it shifted any bedtime before 6am by +1440 minutes so late-night times sort after evening ones on a linear scale, but the shift only applies below the hard 6*60 cutoff — two nearly-identical bedtimes straddling that cutoff (e.g. `05:55` and `06:05`, ten real minutes apart) land ~1430 minutes apart on the scale, tanking the reported consistency score to 0/100 for what's actually a highly consistent bedtime. This is a real design flaw, not just an off-by-one: any single linear cutoff has this discontinuity at the cutoff itself, so nudging the boundary (`<` vs `<=`) only relocates which pair of adjacent minutes breaks, it doesn't fix it — confirmed by hand-checking that the reported example still breaks under the naive boundary tweak. Replaced the linear shift-and-stddev with circular statistics (treat each bedtime as an angle on a 24h clock, compute the mean resultant length `r` of the unit vectors, derive circular standard deviation as `sqrt(-2*ln(r))` in radians, convert back to minutes), which has no seam at any clock hour. Verified against 5 cases via a standalone reproduction: the 05:55/06:05 pair now scores 94 (was 0); three normal cases (consistent ~22:00 bedtimes, a genuine midnight-spanning pair, and genuinely erratic bedtimes) are unchanged from the old function's output, confirming no regression. `node --check` passes on all four core files.
- 2026-08-07: fiftieth pass — delegated a fresh research sweep to a subagent (bugs/v1.1/v1.2+/code-quality still holding per 49 prior passes; areas checked: badge thresholds, auto-updater flow, journal, goal progress math, AI chat streaming, todo sorting — all clean). It found the sibling of yesterday's `sleepConsistency()` fix: `sleepQuality()` (src/renderer.js:2275) has the exact same 6am-cutoff discontinuity — it shifts a bedtime before 6:00 by +1440 minutes onto the same linear scale, but a bedtime *at or after* 6:00 is left unshifted, so `05:55` (shifted to 1795, past the 25:00/1795>1500 "poor" threshold) and `06:05` (unshifted at 365, under every threshold) — ten real minutes apart, same duration — graded 'poor' and 'good' respectively. Reachable via the plain `<input type="time">` bed/wake fields on the Sleep tab with no range restriction, and feeds two visible surfaces: the weekly sleep chart's bar coloring and the Sleep Log list's quality badge. Rather than porting the full circular-statistics approach (overkill for a 3-bucket categorical grade, not a continuous stddev), moved the shift's cutoff from 6am to noon (`bedM<12*60`) so the seam falls at a time no real bedtime is ever near — verified with a standalone reproduction: `05:55`/`06:05` now both correctly grade 'poor'; normal evening (`22:00`→'good'), late (`23:45`→'ok'), and very-late (`00:30`→'ok', unchanged from before since it was already on the shifted side of both the old and new cutoff) bedtimes are unaffected; an edge case the old cutoff also mishandled in the same direction (`11:00`, an unusually late bedtime that used to wrongly grade 'good' since it fell above the old 6am cutoff) now correctly grades 'poor' too. `node --check` passes on all four core files.
- 2026-08-07: fifty-first pass (second today) — delegated a fresh research sweep to a subagent targeting corners not yet covered (calendar/schedule, badge threshold math, XP level curve edges, custom section trackers, goal progress math, sychboard-mcp edge cases, unguarded `parseInt`/`parseFloat`). It found a real bug in `updHol()` (src/renderer.js, Finance tab): the two numeric holiday fields (`target`/`saved`) were parsed with a bare unclamped `parseFloat(...)||0`, unlike every sibling numeric handler in the file (e.g. `updateBal()`, which uses `validateNumber`) — a negative "Saved" value produces a negative progress percentage (`rFinance()`'s `pct` calc only clamps the 100% ceiling, not the floor), which renders as an invalid CSS `width:-N%` on the progress bar (browsers drop the whole declaration, so the bar falls back to full-width) while the caption below it simultaneously reads e.g. "-20% saved" — contradictory, misleading UI state reachable via the plain number input with no `min` attribute. Fixed by switching both fields to `validateNumber(...,0,9999999)`, matching `updateBal()`'s existing pattern. `node --check` passes on all four core files.
- 2026-08-07: fifty-second pass (third today) — delegated a fresh research sweep to a subagent (bugs/v1.1/v1.2+/code-quality all still holding per 51 prior passes), targeting under-audited corners: Calendar/Schedule rendering, badge threshold logic, goal progress math, custom section trackers, the XP level curve, Journal date-keying, and unguarded parseInt/parseFloat. It found a real bug in the AI system-prompt builder's subscription due-date calc, `callGroq()` (src/renderer.js:1319) — the same code area a 2026-08-04 pass fixed a different modulus bug in, but never exercised with billing days 29-31. `due=new Date(y,m,s.date)` (and its next-month rollover branch) passes the raw 1-31 billing day straight into `Date`'s day-of-month field with no bounds check against the target month's real length; when that month is shorter, JS's `Date` silently overflows into the month *after* — not clamps to the last real day — so the calc can skip an entire month. Reproduced: a day-30 subscription checked on Jan 31 2026 rolls `due` to "Feb 30" which JS normalizes to Mar 2, reporting 30 days away and no "DUE IN" warning, when the real next charge is Feb 28 (28 days away) — the whole February occurrence silently vanishes from the proactive-reminder feature this exact code exists for. Fixed by adding a `clampDayOfMonth(y,m,day)` helper (mirrors `localDateStr`'s neighboring style, returns `Math.min(day, new Date(y,m+1,0).getDate())`) and applying it to both the same-month and rolled-over-month day values before constructing `due`. Verified against 5 cases via a standalone reproduction of the exact fixed expression: Jan31→day30 now correctly returns Feb 28 (28d, was Mar 2/30d); Feb27→day31 (non-leap Feb has no 31) returns Feb 28 (1d); Jan30→day29 rolling into non-leap Feb returns Feb 28 (29d); a normal same-month case (Aug7→day15) and a same-day-due case are unchanged. `node --check` passes on all four core files.
- 2026-08-07: fifty-third pass (fourth today) — delegated a fresh research sweep to a subagent (bugs/v1.1/v1.2+/code-quality all still holding per 52 prior passes; areas checked clean this round: sychboard-mcp's less-common tool handlers, main.js's auto-updater IPC wiring, Trips section, Uni exam countdown, badge unlock toast sequencing, AI tool-call bubble rendering, notification permission flow, boot-sequence error paths). It found a real UX bug in `gmCompleteQuest` (src/renderer.js:627-629): a single quest completion can return `sweepBonus>0`, `badgesUnlocked.length>0`, and `streak.freezeUsed===true` all at once (e.g. bridging a 1-day gap with a freeze token, on a completion that both hits a streak-milestone badge and sweeps the last daily quest in its category), and all three cases were wired to `setTimeout(()=>toast(...),800)` at the *same* delay. `toast()` (src/renderer.js:171-176) is a singleton — one shared DOM node/timer, no queueing — so when multiple toasts fire in the same tick each call overwrites the previous one's text/timer, leaving only the last-scheduled message (`❄️ Streak Freeze used`) actually visible; the sweep-bonus and badge-unlock toasts got 0ms of display time despite the XP/coins/badge being correctly recorded in the DB (a lost-notification bug, not a data-correctness one). Rather than just staggering the three call sites' delays (fragile — any future combination of simultaneous toasts elsewhere in the 58 other `toast()` call sites across the codebase would hit the same clobbering), fixed the root cause in `toast()` itself: added a small queue (`_toastQueue`/`_toastShowing`) so concurrent `toast()` calls display sequentially (1800ms visible + 150ms gap each) instead of overwriting each other; the three `gmCompleteQuest` call sites are unchanged since the queue now handles ordering correctly regardless of how they're scheduled. `node --check` passes on all four core files.
- 2026-08-08: fifty-fourth pass — delegated a fresh research sweep to a subagent (bugs/v1.1/v1.2+/code-quality all still holding per 53 prior passes; explicitly steered away from areas already confirmed clean or fixed: Trading212, Journal, sychboard-mcp minor handlers, auto-updater IPC, Trips, Uni exam countdown, badge/toast sequencing, notification permissions, boot errors, Calendar/Schedule, badge thresholds, goal progress math, custom section trackers, XP curve). It found the one remaining un-audited instance of the falsy-zero bug class fixed in `updateDev()` (2026-08-06) and avoided in `updateBal()`: `updateYT()` (src/renderer.js:1933, YouTube tab's manual "Update manually" stat form) parsed the three plain `<input type="number">` fields (`yt-si`/`yt-vi`/`yt-hi`, no `min` attribute) with `parseInt(...)` and gated each assignment on `if(s)`/`if(v)`/`if(h)` — since `parseInt('0')` is `0` and therefore falsy, legitimately entering `0` to correct/reset a stat (e.g. a new channel's subs, or resetting hours) was silently dropped while `save()`/`rYT()`/the "Stats updated" success toast all still fired, leaving the displayed number unchanged with no indication the input was ignored. Grepped ROADMAP.md to confirm this exact call site (`updateYT`/`yt-si`/`yt-vi`/`yt-hi`) was never touched by any prior pass. Fixed by mirroring `updateDev()`'s already-established pattern exactly: read each field's raw trimmed value, treat blank as "leave unchanged," and accept any non-negative parsed integer (including 0) as a real update. `node --check` passes on all four core files.
- 2026-08-08: fifty-fifth pass (second today) — delegated a fresh research sweep to a subagent (bugs/v1.1/v1.2+/code-quality all still holding per 54 prior passes; areas checked clean this round: goal/todo edit flows — no edit feature exists, swipe-gesture sidebar toggle, drag-and-drop section reordering, sparkline/sleep/finance chart math, AI chat-action `.value`/`textContent` sinks, chat bubble rendering, custom-section tracker sanitization, all `ipcMain.handle` entries, YouTube OAuth refresh/expiry, Danielmode easter egg). It found a real mobile-layout bug in `src/styles.css`: the `@media(max-width:720px)` block (active down to `main.js`'s real `minWidth:400` — reachable in the actual desktop Electron window, not just a hypothetical PWA path) already repositions `#toast` to `bottom:76px` specifically to clear the 60px `#mob-nav` bar, but `#update-banner` — same fixed bottom-right corner (`bottom:22px;right:22px`), higher `z-index:500` vs. the nav's `100`, containing live dismiss/restart buttons, shown by `onUpdateAvailable`/`onUpdateDownloaded` (src/renderer.js) until the user acts on it — had no equivalent override. At narrow widths its `max-width:300px` box spans most of the viewport and overlaps the nav's rightmost items (Finance, and "More" — `toggleSidebar()`, the only route to every non-pinned section in mobile layout), so taps there land on the banner instead of the nav for as long as an update notification is showing, effectively locking navigation. Fixed by adding a mobile override in the same `@media(max-width:720px)` block: `#update-banner{bottom:76px;left:12px;right:12px;max-width:none}`, lifting it above the nav bar and letting it span the available width instead of being right-anchored at a size that swallows most of a narrow viewport. Not runnable end-to-end here (no display); verified by reading the exact box geometry (banner span vs. nav item positions) and confirming the fix mirrors `#toast`'s already-proven pattern in the same block. `node --check` passes on all four core files (CSS-only change; JS unaffected).
- 2026-08-08: fifty-sixth pass (third today) — delegated a fresh research sweep to a subagent, told explicitly to read the full log first to avoid retreading the ~9-pass stored-injection sweep (habits/goals/todos/notes/events/shifts/exam-date/username/dev-todos/holiday fields/YouTube titles, 2026-07-26 through 08-01) before searching. It found the one call site that sweep never reached: `bootSend()` (src/renderer.js), the boot-screen "Ask anything..." chat — the very first input surface in the app, before onboarding or login. While waiting for the AI's reply it echoed the user's just-typed message via `respEl.innerHTML=`<span class="boot-word" ...>${msg}</span>`` with `msg` taken raw from `#boot-chat-in`'s `.value.trim()`, no escaping. Typing e.g. `<img src=x onerror="...">` and hitting Enter executes it as live DOM/script immediately, before any network round-trip — same severity class as the ~9 prior injection fixes, just on a call site none of those passes' greps ever matched (`bootSend`/`boot-chat`/`boot-response` never appear anywhere else in the log). The sibling `showBootResponse()` (used for the AI's own replies in the same boot chat) already builds its DOM safely via `textContent`; this was the one place that pattern wasn't followed. Fixed by matching it exactly: clear `respEl`, create the `<span class="boot-word">` via `createElement`, set its style properties directly, and assign the user's text via `.textContent` instead of raw template interpolation. Verified via a standalone Node reproduction contrasting the old `innerHTML` string-build against the new `textContent`-based one for an `<img onerror=...>` payload — old path preserves a live parseable tag, new path fully entity-encodes it. `node --check` passes on all four core files.
- 2026-08-08: fifty-seventh pass (fourth today) — delegated a fresh research sweep to a subagent with an explicit list of the ~50 areas already exhausted (bugs/v1.1/v1.2+/injection sweep/UTF-8 fixes/mcp packaging/falsy-zero fixes etc.), steered toward never-audited surfaces (src/sw.js, manifest.json, netlify edge functions, sychboard-mcp/permissions.json content). It found another instance of the falsy-zero bug class (already fixed twice this week in `updateDev`/`updateYT`) in the Finance tab's Shifts feature, on two related call sites in src/renderer.js: (1) `addShift()`'s wage field did `validateNumber(...)||st.defaultWage` — since `parseFloat('0')` is falsy, entering an explicit wage of `0` (e.g. an unpaid/trial shift, or logging hours only) was silently overwritten with `st.defaultWage` (10), corrupting the stored shift and every earnings total derived from it (`rShifts()`'s all-time/monthly sums, `hours*wage`) with no error or indication to the user; (2) the identical pattern in the AI chat action executor's `add_shift` branch (`wage:a.wage||st.defaultWage`), fed by `parseActions()` collapsing a missing/invalid wage token to `0` at parse time (`parseFloat(w)||0`), so an AI-issued `[ADD_SHIFT:date:hours:0]` had the same bug baked in one step earlier. Fixed both: `addShift()` now reads the raw trimmed field value and only falls back to `st.defaultWage` when it's genuinely blank (`wRaw===''`), otherwise validates the entered number including `0`; `parseActions()`'s ADD_SHIFT regex now maps a non-numeric wage token to `null` instead of `0` (preserving the "not provided" signal), and `executeActions()`'s `add_shift` branch checks `a.wage!=null&&a.wage>=0` before falling back to the default, so an explicit AI-issued wage of `0` now survives while a genuinely missing one still defaults. Verified with a standalone Node reproduction of both code paths: blank field → default wage (10); explicit `'0'` → `0`; normal value → itself; AI wage `'0'` → `0`; AI wage `'abc'`/missing → default. `node --check` passes on all four core files.
- 2026-08-09: fifty-eighth pass — delegated a fresh research sweep to a subagent targeting surfaces the 57 prior passes hadn't reached: `src/sw.js`, `src/manifest.json`, `sychboard-mcp/permissions.json`, `sychboard-mcp/README.md`, `scripts/gen-icons.js`. Service worker, manifest, permissions.json, and the icon generator all came back clean on careful reading (sw.js is dead code in the actual Electron app anyway — registration is gated off for `file:` origin). It found a real doc/reality drift in `sychboard-mcp/README.md`: still described the server as "read-only tools only" with a 3-tool table and a "Phase 2+" out-of-scope note explicitly excluding write/destructive tools, none of which had been touched since `a1580d4` (2026-07-31) added three real, wired-in, confirm-gated write tools (`restart_chuck_bird`, `git_diff`, `git_commit`, `git_push` — 4 tools, 3 of them write) — later confirmed shipped in packaged builds by the 2026-08-02 packaging-fix passes. The README's "Permissions" section was also factually backwards post-`a1580d4`: it said `"confirm"` vs `"auto"` gating "is the client's job," when `server.js`'s own `confirmPermission()`/`confirmCommitMessage()` enforce it server-side (MCP elicitation falling back to a native OS dialog the server spawns itself), specifically so a client can't bypass it — the exact design ROADMAP's own 2026-07-31 audit entry already confirmed. Concrete risk: anyone (a future collaborator, or an AI client doing its own safety check before connecting) reading only the README would conclude this local server is read-only and that gating trusts the client, and could connect an untrusted client or skip auditing the write path — never learning it can SSH-restart the live production Chuck Bird bot or commit/push to real repos, or that the real protection is server-owned. Fixed by updating the Tools table (added the 3 write tools + `git_diff`), the intro line, the Permissions section (corrected to server-side enforcement), the "out of scope" section (moved write tools from excluded to in-scope-and-gated), and `sychboard-mcp/package.json`'s `description` field to match. Documentation-only change, no logic touched; `node --check` passes on all four core files; `package.json` validated via `require()`.
- 2026-08-09: fifty-ninth pass (second today) — delegated a fresh research sweep to a subagent targeting build/packaging config, a surface no prior pass had audited directly. It found `package.json`'s `dependencies` listed real npm packages `three@^0.149.0` and `gsap@^3.15.0`, but the app actually loads Three.js/GSAP exclusively via vendored `<script src="three.min.js">`/`<script src="gsap.min.js">` tags in `src/index.html` — confirmed via grep that no file `require`s/`import`s either as a module and `THREE.` is never referenced. Since electron-builder's `build.files` includes `node_modules/**/*`, both fully-unused packages were dead weight bundled into every packaged installer. Removed both from `package.json` and made the matching edit to `package-lock.json` (dependency entries + `node_modules/three`/`node_modules/gsap` records), verified in sync via `npm ci --package-lock-only --ignore-scripts` (clean, then sanity-checked the check itself catches real mismatches by temporarily adding a bogus dep and confirming it fails). Vendored `src/three.min.js`/`src/gsap.min.js` and their script tags are untouched — runtime behavior unaffected. `node --check` passes on all four core files plus mcp-client.js; `package.json`/`package-lock.json` validated as JSON.
- 2026-08-09: sixtieth pass (third today) — delegated a fresh research sweep to a subagent targeting surfaces still unaudited by any of the prior 59 passes (MEMORY.md, README.txt, .aiexclude, .claude/launch.json, .claude/settings.json, .github/workflows/release.yml beyond its sychboard-mcp npm-ci step, assets/icon.svg, deno.lock, .netlify/ config, scripts/gen-icons.js, and renderer.js areas not previously named). It found an orphaned CI step in `.github/workflows/release.yml`'s `bump` job: `if [ -f preload.js ]; then sed -i "s/|| '[0-9]*\.[0-9]*\.[0-9]*'/|| '$NEW_VERSION'/" preload.js || true; fi` was written to keep the `electronAPI.version` fallback literal (`process.env.npm_package_version || '1.0.16'`) in sync with `package.json` on every release bump — but that field was deleted from `preload.js` as dead code by the 2026-07-31 pass (`da6258a`), which reasonably never thought to check `release.yml` for a step targeting a field it was removing. Verified directly: `preload.js` (read in full) contains no `'[0-9]*.[0-9]*.[0-9]*'`-shaped string anywhere, so the `sed` has matched nothing on every release since 2026-07-31 — harmless (silently no-ops via `|| true`) but dead CI logic masquerading as a real version-sync mechanism, and misleading to a future reader of the workflow file. Removed the line; the surrounding `npm version patch`/git-commit/push steps that actually bump `package.json`'s version are unaffected. Restoring a genuine packaged-app version-sync mechanism (e.g. `app.getVersion()` via IPC) is a separate feature decision, out of scope for this cleanup. Verified `release.yml` still parses as valid YAML (`python3 -c "import yaml; yaml.safe_load(open(...))"`, confirmed 3 steps remain in the `bump` job). `node --check` passes on all four core files (unaffected — YAML-only change).
- 2026-08-09: sixty-first pass (fourth today) — delegated a fresh research sweep to a subagent targeting the CI/deployment config, a surface the ~60 prior passes had never examined. It found that `.github/workflows/release.yml`'s `deploy-pwa` job publishes the same `src/` app to GitHub Pages on every push to `main`, as a second public mirror alongside Netlify — but GitHub Pages has no mechanism to set custom HTTP response headers, so it ships with none of the hardening headers `netlify.toml` sets for the Netlify copy (`X-Frame-Options: DENY`, no enforceable `frame-ancestors` either, since CSP's `frame-ancestors` directive is spec-ignored when delivered via a `<meta>` tag like `src/index.html`'s existing CSP). Net effect: the GitHub Pages mirror has zero clickjacking protection — a malicious page could iframe it and run a click-hijacking overlay against a returning visitor's stored data/actions (e.g. the "Clear all data" button). Distinct from the existing CRITICAL Netlify section (that one is live secret-key exposure via env injection; this is a missing security header on a different, previously-unaudited deployment channel). Whether to keep the GitHub Pages mirror at all is a product decision for Daniel, so left `deploy-pwa` itself untouched; applied the conservative, platform-independent fallback instead — a `<script>if(window.top!==window.self){window.top.location=window.self.location;}</script>` frame-busting guard added as the first script in `src/index.html`'s `<head>`, which works regardless of which static host serves the file and needs no header support. Verified the snippet's syntax directly with `node --check` against an isolated copy (network globals like `window` don't exist in Node, so it can't be checked in-process, but the syntax parses clean). `node --check` passes unchanged on all four core files (index.html is not JS).
- 2026-08-10: sixty-second pass — with random exploration hitting diminishing returns after 61 passes, switched to a systematic re-audit instead: delegated a subagent to grep the whole codebase for remaining live instances of the 6 bug *classes* already fixed repeatedly at different call sites (falsy-zero defaults, UTC/local date mixing, unescaped innerHTML, unescaped `value="${...}"` attributes, month-boundary range filters, unencoded buffer accumulation). 5 of 6 patterns came back clear; it found one real instance of the falsy-zero bug in `src/index.html:497`, the Settings "Default wage" save button — `st.defaultWage=parseFloat(...)||st.defaultWage` silently discarded an explicit `£0` entry (since `parseFloat('0')` is falsy) while the "Wage saved" toast still fired, same bug class already fixed in `updateDev()`/`updateYT()`/`addShift()`'s wage field but missed by prior renderer.js-focused sweeps because this instance lives inline in index.html's `onclick`, not in renderer.js. Fixed by replacing the inline expression with an explicit `isNaN()`/non-negative check (`const v=parseFloat(...);if(!isNaN(v)&&v>=0){st.defaultWage=v;...}`), matching the pattern used at every other falsy-zero fix site. `node --check` passes on all four core files (index.html verified by inspection — attribute is well-formed, entities correctly escaped).
- 2026-08-10: sixty-third pass (second today) — delegated a fresh research sweep to a subagent targeting under-audited corners (scripts/gen-icons.js, .claude/ config, MEMORY.md, sychboard-mcp/permissions.json, and renderer.js functions not previously named). It found one more instance of the falsy-zero-adjacent bug class, this time a missing *lower-bound* guard rather than a falsy-zero drop: `executeActions()`'s `update_yt` branch (`src/renderer.js:1483`, the AI assistant's `[UPDATE_YT:subs|views|hours:N]` action tag) wrote `st.yt[a.field]=a.amount` straight through after only an `!isNaN()` check — `parseActions()`'s `toNum()` helper deliberately preserves a leading `-` in its digit-strip regex, so a value like `[UPDATE_YT:subs:-500]` (plausible if the AI echoes a user saying "I lost subscribers, set it to -500") parses and stores fine as `-500`, while the manual "Update manually" form (`updateYT()`, line 1948, fixed 2026-08-08) already guards `s>=0` on the identical fields. `rYT()`'s monetization progress bar (`sp=Math.min(100,Math.round(st.yt.subs/1000*100))`, line 1922) then renders `width:-50%`, which browsers drop outright since `.pf` has no fallback CSS width — the bar visually falls back to 100%-filled while the adjacent label reads the literal negative count, the same invalid-CSS-width/contradictory-UI shape as the Holidays savings-bar fix (`updHol`, 2026-08-07). Fixed by adding the matching `&&a.amount>=0` guard to the `update_yt` branch, mirroring `updateYT()`'s own bound. `node --check` passes on all four core files.
- 2026-08-10: sixty-fourth pass (third today) — delegated a fresh research sweep to a subagent, explicitly pointed at the full autonomous work log first so it wouldn't retread the ~63 prior passes, and steered toward genuinely unaudited corners (Trips, Uni exam countdown, Calendar/Schedule edge cases, custom section trackers, badge/XP curve math, Journal beyond XSS, notification scheduling edge cases, boot error paths, drag-and-drop/swipe gestures, Danielmode, sychboard-mcp's less-common tools, .claude/ config, MEMORY.md). It found a real instance of the app's own "app-date vs real-date" bug class, one boundary-hour case none of the extensive earlier date-mixing sweeps (2026-07-23, 2026-07-31) had actually closed: `maybeResetHabits()` (src/renderer.js) reset the legacy localStorage habit checkboxes at real local **midnight** (`new Date().toDateString()`), while the SQLite quest/streak system's own day boundary is `day_rollover_hour` (`getAppDate()` in db.js / `computeLocalAppDate()` in renderer.js), defaulting to **4am** with no Settings UI to change it — so on every fresh install, a user who finishes habits before midnight and stays active (or reopens the app) between 00:00-04:00 sees the Quest/Game tab still correctly on "yesterday" (streaks intact, no reset) while the Habits tab has already zeroed every checkbox 4 hours early, a directly contradictory "today" on the same Home screen. `recordHabitHistory()` (the habit heatmap's write path, previously fixed for UTC-vs-local on 2026-07-31 but still keyed to real midnight) had the same boundary, and simply porting `maybeResetHabits()` alone to the rollover-hour basis without also fixing `recordHabitHistory()` would have just relocated the mismatch rather than closed it. Fixed both together via a shared cached `_rolloverHour` (module-level, refreshed once at boot and kept in sync by `checkQuestReset()`'s existing 60s poll — chosen over making `maybeResetHabits()`/`recordHabitHistory()` themselves async, which would have added an IPC-await gap between the boot screen's fade-out and the app actually rendering): `maybeResetHabits()` and `recordHabitHistory()` now both key off `computeLocalAppDate(_rolloverHour)` instead of real local midnight, so the legacy habit system, its heatmap history, and the SQLite quest/streak system all agree on "today." Verified the core date math with a standalone Node reproduction (mocked `Date`/`getHours()` at 1am with rollover=4): old midnight-based logic already reports the next calendar day at 1am (the bug), new rollover-hour-based logic correctly still reports the previous day, and correctly rolls over once actually past 4am. `st.lastHabitReset`'s stored format changes from `toDateString()` to a `YYYY-MM-DD` app-date string; existing users get one harmless extra reset on first load after the update since the old value never matches the new format, the same accepted one-time-reset tradeoff already used for the YT weekly-checklist fix (2026-08-05). `node --check` passes on all four core files.
- 2026-08-10: sixty-fifth pass (fourth today) — closed a gap this morning's own habit-rollover fix (pass 64, above) left open one function away. That fix moved `recordHabitHistory()`'s write key from real local midnight to `computeLocalAppDate(_rolloverHour)`, but `rHabits()`'s heatmap *display* loop, in the same file just above it, still anchored its 30-day list to raw `new Date()` — so between local midnight and the rollover hour (default 4am), the write side and the heatmap's read side disagreed on "today": a habit completed at e.g. 1am correctly writes into yesterday's app-date bucket, but the heatmap's rightmost ("today") box was still keyed to the real calendar date, so it read as "No data" while the actual completion silently showed up one box to the left. Found via a delegated research sweep explicitly steered at corners the 64 prior passes/greps hadn't fully verified. Fixed by anchoring the heatmap loop to the same rollover-shifted date `recordHabitHistory()` uses (mirrors the exact fix shape already used for `getXpHistory()`'s day-list anchor in db.js, 2026-07-20 — anchor the loop to the app-date, not the wall clock). Verified with a standalone Node reproduction: at 1am with rollover=4, the write key and the old heatmap's last box disagreed (`2026-08-10` vs `2026-08-11`); after the fix they match; behavior after the rollover hour is unchanged in both cases. `node --check` passes on all four core files.
- 2026-08-11: sixty-sixth pass — delegated a fresh research sweep to a subagent, steered at genuinely under-audited corners (`.claude/launch.json`/`.claude/settings.json`, `deno.lock`, `assets/icon.svg`/`src/icons/*.svg`, `.netlify/` directory contents, root vs `.netlify/netlify.toml`, a fresh `parseInt`/`parseFloat` grep across renderer.js/index.html, Trips/custom-section-trackers/drag-drop/swipe, and `mcp-client.js`'s stdout handling) after reading the full prior work log to avoid retreading exhausted ground. Everything came back clean except one real dead-reference bug in `src/sw.js`: the PWA service worker's install-time precache list (`SHELL`) still listed `./icons/icon.svg`/`./icons/icon-maskable.svg`, leftover from before the icon pipeline switched to `scripts/gen-icons.js`'s programmatic PNGs — confirmed via grep that neither SVG is referenced anywhere else (not `manifest.json`, not `index.html`, not CSS), while the PNGs `manifest.json`'s `icons` array and `index.html`'s apple-touch-icon actually need (`icon-72/96/128/144/180/192/512.png`, `icon-maskable-512.png`) were never in `SHELL` at all. A 2026-08-09 pass (line 266 above) had looked at `sw.js` and called it clean on the reasoning "dead code in the Electron app" (true — SW registration is gated off for `file:` origin, confirmed in `src/renderer.js`) but missed that the identical `src/` tree is also the live Netlify PWA (`netlify.toml`: `publish = "src"`) and GitHub Pages mirror (`release.yml`'s `deploy-pwa` job), where the SW does register and its cache correctness matters. Net effect on those live deployments: install wastes a round-trip caching two never-requested SVGs while never precaching the icons actually used, so a user who installs the PWA to their home screen and goes offline before the browser has independently fetched those PNGs via the runtime cache gets a missing/broken home-screen icon with no offline fallback for it. Fixed by replacing `SHELL`'s icon entries with the real PNG list matching `manifest.json`, and bumping `CACHE` to `'sychboard-v3'` so returning visitors get the corrected precache set instead of keeping their stale v2 cache. Not runnable end-to-end here (no browser/service-worker environment); verified by cross-referencing `manifest.json`'s icon list, `index.html`'s apple-touch-icon reference, and a repo-wide grep confirming the old SVG paths are dead everywhere except `sw.js` itself. `node --check` passes on all four core files plus `src/sw.js`.
- 2026-08-11: sixty-seventh pass (second today) — delegated a fresh research sweep targeting the Journal feature end-to-end, one of the corners explicitly flagged as under-audited despite being searched by name in ~4 prior passes (each time a different bug was found elsewhere first). Found a real instance of the app-date-vs-real-date mixing bug class: all three Journal call sites (`callGroq()`'s `todayJournal` context line, `rJournal()`'s title/textarea/past-entries split, `saveJournal()`) keyed `st.journals` by real local `new Date().toDateString()`, never migrated to the `day_rollover_hour`-aware app-date convention every other "today" concept in the app now uses (SQLite quests/streaks via `getAppDate()`; legacy Habits via `computeLocalAppDate(_rolloverHour)` as of the 2026-08-10 fix). Failure window: between local midnight and the rollover hour (default 4am), Habits/Quests/Home still correctly treat it as "yesterday," but Journal already flips to a new calendar-day key — a journal entry written a few hours earlier that same app-evening goes "missing" from the textarea (reads as a blank new day, silently demoted to a past entry) while the rest of the dashboard agrees it's still the same day, and the AI system prompt's `todayJournal` context wrongly reports "nothing written today." Verified the exact failure window with a standalone Node reproduction (mocked `Date` at 1am, rollover=4): old `toDateString()` key ("Tue Aug 11 2026") already disagreed with `computeLocalAppDate(4)` ("2026-08-10") at that hour. Fixed by switching all three sites (`src/renderer.js:1329,2256,2275`) to `computeLocalAppDate(_rolloverHour)`, the same helper already used by the habit-reset fix. Accepted one-time tradeoff (same as the habit-reset/YT-checklist fixes): the stored key format changes from `toDateString()` to `YYYY-MM-DD`, so existing users' most recent entry (if written before this update) won't match a future "today" lookup and displays as a past entry instead — nothing is lost, `Object.entries(st.journals)` still lists it by its original key. `node --check` passes on all four core files.
- 2026-08-11: sixty-eighth pass (third today) — delegated a fresh research sweep, steered at genuinely unaudited corners after reading the full 67-pass log first. It found the one sibling the Habits/Journal rollover-hour migrations (2026-08-10/08-11) hadn't reached: the Sleep tracker. All five of its "today" call sites (`logSleep()`, `renderSleepChart()`'s day-list loop and `isToday` check, `rSleep()`, and the AI assistant's `[LOG_SLEEP:...]` action-tag handler in `executeActions()`) still keyed off real local `localDateStr(new Date())`, never migrated to the `computeLocalAppDate(_rolloverHour)` convention Habits/Journal/Quests all now share — same bug class, same file, just the one tracker left behind. Failure window: with the default 4am rollover, between local midnight and 4am the rest of the dashboard still correctly calls it "yesterday" while Sleep already flips to the new calendar day — logging sleep (manually or via the AI) in that window files the entry a day ahead of what every other tracker calls "today," and the chart's "today" bar/heatmap-style highlight disagrees with the app-wide app-date. Verified with a standalone reproduction (mocked `Date` at 2026-08-12 01:00, rollover=4): app-wide today reported `2026-08-11`, old Sleep `today` reported `2026-08-12` (mismatch), new Sleep `today` now also reports `2026-08-11`. Fixed by switching `logSleep`/`rSleep`/the AI handler to `computeLocalAppDate(_rolloverHour)` directly, and mirroring the habit-heatmap's existing rollover-adjusted-base-date pattern (`src/renderer.js:2168-2171`) in `renderSleepChart()`'s 7-day loop and `clearOldSleepLogs()`'s 30-day cutoff so their day-walks anchor to the app-date, not the wall clock. `node --check` passes on all four core files.
- 2026-08-11: sixty-ninth pass (fourth today) — delegated a fresh research sweep, again reading the full log first to avoid retreading it. It found the last sibling of the app-date-vs-real-date migration (Habits 2026-08-10, Journal/Sleep 2026-08-11 above): the Schedule tab and the Home "Upcoming" widget. `rSchedule()` (`src/renderer.js:2128`) and `renderHomeGamification()`'s Upcoming card (`:904`) both derived "which weekday is today" via raw `new Date().getDay()`, never migrated to the rollover-hour-aware app-date every other tracker now shares. Failure window: with the default 4am rollover, between local midnight and 4am the rest of the dashboard (streaks, daily quests, journal, sleep) still correctly treats it as yesterday, but the Schedule grid highlights *tomorrow's* day cell and the Home Upcoming card lists *tomorrow's* events — a visibly self-contradictory "today" on the same screen. Verified with a standalone reproduction (mocked `Date` at Wed Aug 12 2026 01:00, rollover=4): app-date-consistent weekday is Tuesday, old `getDay()`-based index reported Wednesday; after the rollover hour (5am) both old and new logic agree, confirming no regression outside the boundary window. Fixed by adding `appWeekdayIndex()` (mirrors `computeLocalAppDate(_rolloverHour)`, converts the resulting date string back to a Mon=0..Sun=6 index via a local-midnight `Date`) next to `computeLocalAppDate` in src/renderer.js, and switching both call sites to it in place of the raw `new Date().getDay()` expression. `node --check` passes on all four core files.
- 2026-08-12: seventieth pass — delegated a fresh research sweep to a subagent (told to read the full ~69-pass log first and steer at genuinely unaudited angles: badge unlock conditions, Danielmode, AI streaming/abort, goal completion edge cases, todo sorting, auto-updater flow, OAuth refresh math, quit lifecycle, keyboard shortcuts, export/import round-trip of badge_unlocks). It found the one call site the previous day's own Schedule/Home weekday-rollover fix (pass 69, above) missed: `callGroq()` (src/renderer.js:1332), the AI chat system-prompt builder, still computed "today's weekday" via raw `now.getDay()` instead of the new `appWeekdayIndex()` helper — so between local midnight and the rollover hour (default 4am), asking the AI assistant "what's on my schedule today" would report *tomorrow's* events as today and mark the wrong day `(TODAY)` in the full-week schedule context it sends the LLM, directly contradicting what the Schedule tab and Home widget show on screen at that same moment (both already fixed to agree with `appWeekdayIndex()` the day before). Fixed by switching line 1332 to call `appWeekdayIndex()` (safe to call before its later declaration in the file — it's a hoisted `function` declaration, same as every other call site already using it), leaving `now`/`timeStr` on the wall clock since those correctly represent the actual time-of-day shown to the AI. `node --check` passes on all four core files.
- 2026-08-12: seventy-first pass (second today) — delegated a fresh research sweep to a subagent (read the full 70-pass log first, steered at genuinely unaudited corners: Trips section, custom section trackers, badge/XP curve math, goal progress math, keyboard shortcuts, export/import round-trip of badge_unlocks, Danielmode, sychboard-mcp's less-common tool handlers). It found an unguarded numeric bound in the AI assistant's `[ADD_TRIP:destination:date:budget]` action tag: `parseActions()` (src/renderer.js:1479) parsed `budget` with a bare `parseFloat(budget)||0`, unlike the sibling AI-action-tag guards in the same `executeActions()` function (`update_yt`'s `a.amount>=0`, `add_shift`'s `a.wage>=0`, `add_subscription`'s `a.amount>0`) and unlike the manual "Add Trip" form (`addTrip()`, line 2218), which clamps the identical field via `validateNumber(...,0,99999)`. A trip logged through the AI with a negative budget (e.g. `[ADD_TRIP:Paris:2026-09-01:-500]`) stored the negative value unmodified and `rTravel()` (line 2212) rendered it straight through `fmt()` as the nonsensical `Budget: £-500.00`, with no invalid-CSS-width side effect this time (plain text, not a progress bar) but the same "AI action-tag path skips a bound the manual form enforces" shape as the fixed siblings. Fixed by clamping at the parse site: `Math.min(Math.max(parseFloat(budget)||0,0),99999)`, matching the manual form's own range. `node --check` passes on all four core files.
- 2026-08-12: seventy-second pass (third today) — delegated a fresh research sweep to a subagent (read the full 71-pass log first; ruled out AI streaming/abort, goal progress %, and todo sort-by-date as nonexistent features rather than force a finding there, then traced badge thresholds, OAuth refresh expiry math, and the quit lifecycle clean before landing on this). It found that `importGameData()`'s three timestamp fallbacks (`src/db.js`, `quest_completions.completed_at`/`xp_log.occurred_at`/`badge_unlocks.unlocked_at`, used when a restored backup's row is missing that field) wrote `new Date().toISOString()` — but every real row in these columns is written by SQLite's own `datetime('now')`, which produces `"YYYY-MM-DD HH:MM:SS"` (space-separated, no `T`/`Z`/milliseconds), the exact shape `parseUtcTimestamp()` (src/renderer.js:955) is built to convert (`s.replace(' ','T')+'Z'`). `toISOString()` already has a `T` and trailing `Z`, so nothing gets replaced and `parseUtcTimestamp` appends a *second* `Z`, producing `"Invalid Date"` — reproduced directly: `new Date('2026-08-12T15:34:08.000Z'.replace(' ','T')+'Z')` → `Invalid Date`. Concrete failure: restoring a backup (the existing "Restore from Backup…" flow, `data:import-game`) where a `quest_completions`/`xp_log` row is missing its timestamp (a plausible partially-corrupted or hand-edited file — exactly the input class `importGameData`'s shape-validation is meant to tolerate rather than reject) hits this fallback, then the Home Recent Activity feed's `_timeAgo(parseUtcTimestamp(a.completed_at))` (renderer.js:928) computes `Date.now()-NaN` and renders **"NaNd ago"**; the identical `badge_unlocks.unlocked_at` fallback shows **"unlocked Invalid Date"** in the Achievements tooltip (renderer.js:1059). This is a fallback path added 2026-07-28 alongside `importGameData` itself and never cross-checked against `parseUtcTimestamp`'s format assumption by any of the ~10 prior UTC/local-date-mixing passes (2026-07-23 onward), since those all touched *live-write* call sites, not this one *import-fallback* site. Fixed by adding a `sqliteNow()` helper (next to `formatLocalDate`/`parseLocalDate`) that produces the same `"YYYY-MM-DD HH:MM:SS"` shape via `new Date().toISOString().slice(0,19).replace('T',' ')`, and swapping all three fallbacks to use it. Verified directly: the old fallback shape now confirmed `Invalid Date` under `parseUtcTimestamp`, the new one parses correctly and matches `/^\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}$/` (SQLite's own format). `node --check` passes on all four core files.
- 2026-08-13: seventy-third pass — delegated a fresh research sweep to a subagent (read the full 72-pass log first and was given the exhaustive list of already-fixed bug classes/checked-clean areas to avoid retreading). It found a real farming exploit in `completeQuest()`/`uncompleteQuest()` (src/db.js), a grant-side sibling of the freeze-token *consumption* leak fixed 2026-07-21: the 7-day-streak-milestone reward (`if (newStreak % 7 === 0 && newStreak > currentStreak) tokens = Math.min(tokens+1,3)`, line 411) grants a Streak Freeze token with no record of which completion granted it, while `uncompleteQuest()` only ever refunds a token when the completion being undone *consumed* one to bridge a gap (`freeze_used`) — it never claws back one *granted* by that same completion. Repeatedly completing the quest that pushes a category's streak to a fresh multiple of 7 and then immediately uncompleting it reverts the streak cleanly back to 6 every time (correct) but leaves the granted token in place (not reverted), so 3 toggle cycles net 3 free tokens per category — up to 12 across the 4 categories, capped only by the 3-per-category ceiling, for zero elapsed time and zero real progress. Fixed by mirroring the existing `freeze_used` mechanism on the grant side: added migration v6 (`quest_completions.milestone_token_granted`), set it in `completeQuest` when the milestone branch actually increments tokens (guarded by `tokens < 3` so hitting a milestone while already capped — a true no-op — isn't recorded as a clawback-able grant), and subtracted it symmetrically alongside the existing freeze-refund term in `uncompleteQuest`'s streak UPDATE (`freeze_tokens=MAX(0,MIN(3,freeze_tokens+refund-clawback))`). Also carried the new column through `exportGameData`/`importGameData` for backup round-trip fidelity, matching how `freeze_used` is already handled there. Verified against a real SQLite DB (`node:sqlite` shim): the exact 3-cycle farm repro now correctly ends at `{current_streak:6, freeze_tokens:0}` instead of ratcheting to 3; a control case where tokens are already at the 3-cap when hitting a milestone still nets `{streak:6, tokens:3}` after complete→uncomplete (no over-subtraction below what was already there); a genuine 2-day-gap freeze-consumption bridge (unrelated code path) still refunds its token correctly on uncomplete, confirming no regression to the 2026-07-21 fix. `node --check` passes on all four core files.
- 2026-08-13: seventy-fourth pass (second today) — delegated a fresh research sweep to a subagent (given the full exhausted-areas list from the 73-pass log to avoid retreading). It found a real ownership-check gap in the shop *equip* path, the sibling of the already-fixed shop *purchase* trust gap (2026-07-28/08-10): `equipShopItem()` (src/renderer.js) wrote a chosen cosmetic straight through the generic `settings:set` IPC channel (`window.sychboard.settings.set('equip_'+item.type, key)`) with no ownership check anywhere — not client-side (the function never called the same `shopItemOwned()` helper its own button-rendering logic uses to decide Buy-vs-Equip), and not server-side (`settings:set` in main.js only ever validated the `day_rollover_hour` key; `db.setSetting()` is a blind `INSERT OR REPLACE`). Concretely: with 0 SychCoins and nothing purchased, opening DevTools in the packaged app (the default Electron menu's Toggle Developer Tools works there, not just in dev — `!app.isPackaged` only gates *auto-opening* devtools at startup) and running `window.sychboard.settings.set('equip_bg','bg_aurora')` (a 150-coin item) equipped and persisted it instantly via `loadEquips()`/`applyEquips()` on every future launch, with zero coins spent and `shop_owned` never touched — a complete bypass of the SychCoins economy the purchase-side hardening exists to protect. Fixed by adding a dedicated, ownership-gated write path mirroring `purchaseItem()`'s pattern: extended `SHOP_CATALOG` (src/db.js) from a flat cost map to `{cost,type}` entries, added `equipItem(itemKey)` (rejects unknown keys; for any item with `cost>0`, rejects unless present in `shop_owned`; only then writes `equip_<type>`), a new `shop:equip` IPC handler (main.js) and `shop.equip` preload bridge, switched `equipShopItem()` to call it (plus a client-side `shopItemOwned()` guard as defense-in-depth, matching the button logic), and closed the direct-write hole itself by rejecting any `equip_*` key in `settings:set` so the guarded path can't be routed around. Verified against a real SQLite DB (`node:sqlite` shim): equipping an unpurchased paid item is rejected (`not_owned`), a free item equips with no ownership needed, an unknown key is rejected, and equipping the same item after a real purchase (with coins correctly deducted) succeeds. `node --check` passes on all four core files.
- 2026-08-13: seventy-fifth pass (third today) — delegated a fresh research sweep to a subagent (given the full 74-pass exhausted-areas list to steer clear of). It found the most severe single gap in a long time: the "Restore from Backup…" flow (`handleImportFile()`, src/renderer.js, added 2026-07-28) writes every `sb4_*` key from an uploaded JSON file straight into `localStorage.setItem(k,v)` verbatim, then `load()` copies those values straight into `st.*` with no re-validation — completely bypassing every one of the ~10 `sanitizeText()`-on-write stored-XSS fixes made across 2026-07-26 through 08-08 (trips/habits/goals/todos/notes/events/shifts/exam-date/username/dev-todos/holiday fields/boot chat), since those all sanitize at the specific UI input handler that produces the value, not at render time — the render sites (e.g. `rTravel()`'s raw `${t.dest}` in `innerHTML`) all still trust whatever's already in `st`. A backup file is exactly the "corrupted/hand-edited/untrusted file" input class this app's own design already treats as adversarial elsewhere (`importGameData`'s shape validation, the FK-violation rollback test) — but the localStorage half of restore never got the same treatment. Concretely: a crafted backup with `sb4_trips` containing `dest:"<img src=x onerror=alert(document.cookie)>"`, imported via the ordinary Settings → Restore from Backup… button (a file someone might share or a hand-edited real backup), executes as live script in this privileged Electron renderer (contextBridge to filesystem/SQLite/OAuth tokens/git-write MCP tools) the moment the Travel section renders — silently re-opening every one of the ~10 prior fixes at once through one overlooked entry point. Fixed by adding `sanitizeImportedValue(v)` (recursively applies the existing `sanitizeText()` escape to every string leaf in a parsed JSON value, arrays/objects walked, 10-level depth cap) and routing every imported `sb4_*` value through it before writing to `localStorage`, except `sb4_journals`/`sb4_chatHistory` — deliberately left raw since those two are HTML-escaped at render time instead (`parseMD()`, `msgBubble()`), so re-sanitizing here would double-encode a legitimate `<`/`>` a user typed in a journal entry or chat message. Malformed/non-JSON values for a given key are skipped (not stored) rather than risking unsafe or unparseable data. Verified with a standalone reproduction: the `<img onerror=...>` trips payload above is fully neutralized (entity-escaped, no raw `<img` survives); a normal round-trip of already-escaped legitimate data (e.g. an apostrophe in a trip name) passes through idempotently with no double-escaping; numeric/boolean fields (`yt.subs`, `analyticsConnected`, etc.) are preserved byte-for-byte. `node --check` passes on all four core files.
- 2026-08-13: seventy-sixth pass (fourth today) — delegated a fresh research sweep to a subagent (given the full 75-pass exhausted-areas list). It found a dead, misleading form field in the Dev/Side-Project tab: `src/index.html:370`'s Stats card rendered a free-text `<input id="dev-s1i" placeholder="Status">` sitting directly next to the real status `<select id="dev-si">` — but `updateDev()` (src/renderer.js) only ever read `dev-m1i` and `dev-si`, never `dev-s1i`. Confirmed via grep it's referenced nowhere in any JS file and via `git log -S` that it's been unwired since the very first commit, predating even the pre-SQLite renderer.js split. A user typing into the plausibly-labeled "Status" box (right next to a same-purpose dropdown) had their input silently discarded on "Update" while the success toast fired unconditionally — a false-positive save on a field that never did anything, distinct from the app's many already-fixed falsy-zero bugs (those keep the old value on an edge case; this one always discards input, unconditionally). Fixed by removing the dead `dev-s1i` input from the markup, leaving the real `dev-si` dropdown as the sole status control. `node --check` passes on all four core files (index.html reviewed by inspection, not JS).
- 2026-08-13: seventy-seventh pass (fifth today) — delegated a fresh research sweep to a subagent (given the full 76-pass exhausted-areas list to steer clear of). It found the one call site the 2026-08-10 habit-rollover migration (passes 64-65) didn't reach: `rHabits()`'s date header (`src/renderer.js:2185`, feeds `#habit-date` next to the "Reset day" button) still rendered `new Date().toDateString()` — real wall-clock date — while the same function's heatmap loop a few lines below (fixed pass 65) and `recordHabitHistory()`/`maybeResetHabits()` (fixed pass 64) all correctly use the rollover-hour-aware app-date. Failure window: with the default 4am rollover, between local midnight and 4am the header reads e.g. "Thu Aug 13" while the heatmap's own rightmost box and the streak system both still correctly treat it as "Wed Aug 12" — a self-contradictory date on the same card. Fixed by hoisting the existing rollover-aware `base` date computation (previously computed later, only for the heatmap loop) to the top of `rHabits()` and reusing it for the header text, removing the now-duplicate second computation. `node --check` passes on all four core files.
- 2026-08-14: seventy-eighth pass — delegated a fresh research sweep to a subagent (given the full 77-pass exhausted-areas list to steer clear of, pointed at Calendar/Schedule beyond the weekday-rollover fix, Goals, Todo sorting, Trading212 dead paths, badge edge cases, AI chat context management). It found the one call site the 2026-08-12 Schedule/AI-weekday-rollover fix (pass 70) didn't reach: `callGroq()`'s own SCHEDULE block (`src/renderer.js:1397`, the AI system-prompt builder) named "today" via raw `now.toLocaleDateString('en-GB',{weekday:'long'})` — real wall-clock weekday — while `todayEvs` and `weekSched`'s `(TODAY)` marker on the very same lines (1349/1351) already correctly use the rollover-hour-aware `ai=appWeekdayIndex()` (fixed pass 70 was `now.getDay()` at this same function, one line up, but only for the weekday *index* used by `evs`/`ai`, not this separate weekday-*name* string one line below it). Failure window: with the default 4am rollover, between local midnight and 4am the AI's system prompt reads self-contradictory, e.g. "Today (Wednesday, ...): <Tuesday's events>" with "Tue (TODAY)" in the week grid right below it — the AI assistant could answer "what's on today" with the wrong day's plans during that ~4-hour window daily. Fixed by deriving the weekday name from the same app-date already computed for `ai` (`computeLocalAppDate(_rolloverHour)` → local-midnight `Date` → `toLocaleDateString`) instead of from `now` directly; `timeStr` (real wall-clock time, shown alongside) is unchanged, matching the existing pattern of pairing an app-date-scoped day name with the real current time. Verified with a standalone reproduction (mocked Wed Aug 12 2026 01:15, rollover=4): old code returned "Wednesday", new code returns "Tuesday" (matching `ai`'s app-date), and a post-rollover check (05:00) confirms old/new agree once past the rollover hour, no regression. `node --check` passes on all four core files.

- 2026-08-14: seventy-ninth pass (second today) — delegated a fresh research sweep to a subagent (given the full 78-pass exhausted-areas list to steer clear of). It found the backup-restore sibling of the 2026-08-13 shop-equip ownership fix: `equipItem()` (src/db.js) gates the live `shop:equip` IPC path on `shop_owned`, and `settings:set` (main.js) separately rejects any direct `equip_*` write — but `importGameData()`'s settings-restore loop (src/db.js:827-828) writes every `{key,value}` pair from an imported backup straight into the `settings` table via `INSERT OR REPLACE`, with no equivalent ownership check, and `loadEquips()`/`applyEquips()` (src/renderer.js) read `equip_*` straight from settings with no check of their own either — that check only ever lived in the now-closed live-write paths. A crafted or hand-edited backup file (`sychboard_backup:true` JSON, the same "Restore from Backup…" entry point already treated as adversarial input elsewhere in this exact function) with `equip_bg:"bg_aurora"`/`equip_accent:"accent_gold"` and `shop_owned:"[]"` equips every paid cosmetic for free on import, a full bypass of the SychCoins economy the 2026-08-13 fix specifically closed for the live path. Fixed by adding a post-restore validation pass in `importGameData()`: after `shop_owned` is finalized, each restored `equip_<type>` setting is checked against `SHOP_CATALOG` (must exist, match its slot type, and — if it costs coins — appear in the restored `shop_owned` list) and deleted from `settings` if it fails, letting the renderer's existing `EQUIP_DEFAULTS` fallback apply instead of silently trusting an unowned equip. Verified against a real SQLite DB (`node:sqlite` shim): a malicious import with three unowned paid equips (`bg_aurora`, `accent_gold`, `font_mono`) and empty `shop_owned` has all three stripped to `null` post-import; a control import where `bg_aurora` is genuinely listed in `shop_owned` keeps it equipped, and a free item (`font_grotesk`, cost 0) always survives regardless of ownership. `node --check` passes on all four core files.

- 2026-08-14: eightieth pass (third today) — delegated a fresh research sweep to a subagent (given the full 79-pass exhausted-areas list to steer clear of, pointed at Goals progress math, Todo sorting, Trading212 dead paths, badge edge cases, AI chat context management, keyboard shortcuts, Danielmode, custom section tracker arithmetic, drag-and-drop reordering). It found an unbounded `day` field in the AI assistant's `[ADD_SUBSCRIPTION:name:amount:day]` action tag (`parseActions()`, src/renderer.js:1501): `day:parseInt(day)||1` had no 1-31 clamp, unlike the manual "Add Subscription" form (`addSub()`, line 1937: `validateNumber(...,1,31)`) and every other sibling AI-action-tag numeric field already bounded in prior passes (`add_trip`'s budget, `update_yt`'s amount, `add_shift`'s wage, this same tag's own `amount`) — same "AI tag path forgot the bound the manual form enforces" shape fixed four times before, just never checked for this specific field. A plausible AI reply like `[ADD_SUBSCRIPTION:Netflix:10.99:35]` (the tag's own doc comment never tells the LLM the day must be 1-31) stores `date:35` verbatim: `rSubs()` renders the nonsensical "Day 35", and `callGroq()`'s own due-date reminder (line 1375) only clamps the *upper* bound via `clampDayOfMonth()` — a negative day would roll `new Date(y,m,-N)` into the previous month, silently corrupting the proactive subscription-warning feature already fixed twice before (2026-08-04, 2026-08-07) for unrelated month-math bugs in this exact block. Fixed by matching the manual form's bound: `day:Math.min(Math.max(parseInt(day)||1,1),31)`. `node --check` passes on all four core files.

- 2026-08-14: eighty-first pass (fourth today) — delegated a fresh research sweep to a subagent, explicitly steered at the areas the 80th pass named as still under-audited (Goals progress math, Todo sorting, badge unlock edge cases, AI chat context management, keyboard shortcuts, Danielmode, custom section tracker arithmetic, drag-and-drop reordering). All eight came back clean or nonexistent-as-features (no goal %, no todo-sort function, no keyboard shortcuts beyond Enter-to-send, custom-section "trackers" are free text with no arithmetic, drag-and-drop reorder logic is correct). It found one more real instance of the "AI action-tag path skips a bound the manual form enforces" class (fixed 4 times before for `add_trip`'s budget, `update_yt`'s amount, `add_shift`'s wage floor, `add_subscription`'s day) at a call site none of those touched: the AI's `[ADD_SHIFT:date:hours:wage]` tag (`parseActions()`, src/renderer.js:1495) had no upper bound on `hours` at all (only `executeActions()`'s `a.hours>0` floor gate) and only a floor on `wage` (`a.wage>=0`, fixed 2026-08-08 for the falsy-zero class), while the manual "Add Shift" form (`addShift()`, line 1919/1921) clamps both via `validateNumber(...,0,24)`/`validateNumber(...,0,100)`. A plausible AI reply like `[ADD_SHIFT:2026-08-14:80:15]` (misreading "worked 80 hours this fortnight" as one shift) stored `hours:80` verbatim, and `rShifts()`'s `hours*wage` earnings math folded a nonsensical £1200 single-shift entry into the Finance tab's all-time/monthly totals with no validation — the same inflated figure then poisons `callGroq()`'s own system-prompt shift context on later turns. Fixed by matching the manual form's bounds exactly: `hours:validateNumber(h,0,24)`, `wage:isNaN(wp)?null:validateNumber(wp,0,100)` (preserving the existing `wage:null`-for-missing signal from the 2026-08-08 fix). Verified `validateNumber`'s own `parseFloat(val)||0` behavior doesn't reintroduce a falsy-zero bug here since it's fed an already-parsed number, not a raw string — `validateNumber(0,0,100)` correctly returns `0`. `node --check` passes on all four core files.

**Assessment for Daniel:** this is the 4th pass today and the 81st overall since 2026-07-18. The bugs/v1.1/code-quality queue has been re-confirmed exhausted since roughly 2026-07-25, and every self-contained v1.2+ item is done — only YouTube API/Trading 212 (blocked on real keys) remains on that list. Findings have narrowed to individual unbounded/unvalidated fields on sibling code paths (AI action-tag vs. manual form) rather than new bug classes — genuinely marginal at this point, though each has still been a real, fixable gap. The 🚨 CRITICAL Netlify API-key-exposure item (flagged 2026-08-01, still unresolved) remains the highest-value thing left in this file and is explicitly outside this routine's scope (needs Daniel's own decision on key rotation + site-privacy status), as is the merge-to-main decision itself.
- 2026-08-15: eighty-second pass — delegated a fresh research sweep to a subagent (given the full 81-pass exhausted-areas list to steer clear of, pointed at badge unlock logic, XP level curve edges, AI chat streaming/error handling, remaining unaudited `.value`/`parseInt`/`parseFloat` call sites, and export/import round-trip fidelity). It found the Finance tab's "Add Bill" form, `addSub()` (src/renderer.js:1934) — the sibling of the already-fixed `updateBal()`/`addShift()` blank-field gaps, but inverted: its `!n||!a||!d` required-field check called `validateNumber(val,0.01,99999)`/`validateNumber(val,1,31)` directly on the raw input, and since `validateNumber` clamps a blank/unparseable value's `parseFloat(...)||0` result *up* to the given `min` (0.01 and 1 respectively) rather than leaving it at `0`, `a`/`d` could never actually be falsy — the "Name, amount, and day required" toast only ever fired for a blank name. Leaving Amount/Day blank (no `required` HTML attribute on either field) and clicking "Add Bill" silently pushed a bogus `{amount:0.01, date:1}` subscription instead of rejecting the input, polluting the finance totals, the "due in Xd" reminder list, and the AI chat's finance context with fabricated data. Fixed by checking the raw trimmed `sub-amt`/`sub-day` strings for emptiness before clamping (mirroring `updateBal()`'s existing blank-check pattern) so a blank field now correctly evaluates to `0`/falsy and trips the required-field toast; a real `0.01`/day-`1` entry (the legitimate minimum) still passes through unaffected. Verified with a standalone reproduction of the exact validation expression: blank amount+day now correctly rejects, blank name still rejects, a fully-filled form still succeeds, and an explicit boundary-minimum entry (`0.01`/day `1`) still succeeds. `node --check` passes on all four core files.
- 2026-08-15: eighty-third pass (second today) — delegated a fresh research sweep to a subagent (given the full 82-pass exhausted-areas list to steer clear of, pointed at badge unlock edges, XP curve, AI chat streaming, keyboard shortcuts, Danielmode, OAuth refresh math, and sychboard-mcp's less-common tools). It found the AI assistant's `[SET_BALANCE:bank|savings|trading:N]`/`[ADD_BALANCE:...:N]` action tags (`parseActions()`/`executeActions()`, src/renderer.js:1487-1488/1514-1515) had no bound at all on the parsed amount — only `!isNaN(a.amount)` — unlike the manual "Update Balance" form (`updateBal()`, line 1902: `validateNumber(...,0,9999999)`), the same "AI action-tag path forgot the bound its sibling manual form enforces" class fixed five times before (`add_trip` budget, `update_yt` amount, `add_shift` wage/hours, `add_subscription` day) but never previously checked for this field. A plausible AI reply like `[SET_BALANCE:trading:-2000]` (e.g. describing margin debt) or an absurdly large value stored an out-of-range balance directly, corrupting the Finance tab's Total, the Home wealth stat/sparkline, and feeding the corrupted figure back into the AI's own system-prompt finance context on later turns. Fixed `set_balance` (an absolute assignment, architecturally identical to `updateBal()`) by clamping at parse time with the same `validateNumber(v,0,9999999)`; left `add_balance`'s parsed delta itself unclamped (it's documented as signed — "negative to subtract" — so bounding the delta would break legitimate subtraction) and instead clamped the *result* in `executeActions()` to `[0,9999999]` after applying it, so the stored balance can't land outside the manual form's range regardless of how it got there. Verified with a standalone reproduction: `SET_BALANCE:-2000`→0, `SET_BALANCE:99999999999`→9999999, a normal `1500`→1500 unchanged; `ADD_BALANCE` of `-200` on a balance of 500 still correctly nets 300 (legitimate subtraction preserved), an oversized negative delta floors at 0, an oversized positive delta ceilings at 9999999. `node --check` passes on all four core files.
- 2026-08-15: eighty-fourth pass (third today) — found one more instance of the "AI action-tag path skips a bound the manual form enforces" class: the AI's `[LOG_SLEEP:bedtime:waketime]` tag (`parseActions()`, src/renderer.js) padded whatever text the LLM wrote into a time string with no numeric/range check at all, unlike the manual Sleep form's native `<input type="time">` (which can only ever submit a valid `00:00`-`23:59` string). `sleepDuration()`/`sleepQuality()`/`sleepConsistency()` all do real arithmetic on bed/wake (`h*60+m`), so a non-numeric component (e.g. a hallucinated `[LOG_SLEEP:Late:30:07:15]`) silently produced `NaN` duration/quality shown on Home and the Sleep tab, and an out-of-range hour (e.g. `28:45`) silently computed a wrong-but-plausible duration (verified: `2.25h`) instead of being rejected. Fixed by validating all four captured components as real 0-23/0-59 integers before pushing the action; an invalid tag is now dropped (no-op), matching how `add_event` already drops an unrecognised day. Verified with a standalone reproduction: normal input and the `00:00`/`23:59` boundary still parse correctly; non-numeric hour, out-of-range hour, and out-of-range minute are all correctly dropped instead of storing corrupted data. `node --check` passes on all four core files.
- 2026-08-15: eighty-fifth pass (fourth today) — delegated a fresh research sweep to a subagent (given the full 84-pass exhausted-areas list to steer clear of). It found a real, destructive bug in five AI chat action tags: `complete_habit`/`complete_goal`/`delete_todo`/`remove_habit`/`remove_event` (`parseActions()`/`executeActions()`, src/renderer.js) all locate their target via `str.toLowerCase().includes(a.val)` partial-match, but nothing checked that `a.val`/`a.name` was non-empty after `.trim()` — the tag regexes only require one character between the colon and `]`, so a slightly malformed AI reply (e.g. `[REMOVE_HABIT: ]`, a lone space) trims to `''`, and `anything.includes('')` is always `true` in JS. For the three destructive branches this doesn't just mis-target, it wipes: `delete_todo` empties the entire general todo list, `remove_habit` deletes every habit, and `remove_event` clears every event for that day, all silently and with no confirmation (unlike the app's other bulk-affecting actions — backup restore, clear-all-data — which do confirm first). `complete_habit`/`complete_goal` have the milder version: an empty match just completes the first habit/goal in the list instead of the intended one. This is a normal LLM failure mode (stray whitespace, or the model not confidently recalling the exact item text), not a contrived attack, and `executeActions()` runs automatically on every AI reply. Fixed by adding an `&&a.val`/`&&a.name` guard to each of the five `else if` conditions, matching the existing guard style already used for `add_shift`/`add_subscription` (`&&a.hours>0`/`&&a.amount>0`) — a safe no-op on the degenerate empty-match case, no change to any legitimate use. `node --check` passes on all four core files.
- 2026-08-16: eighty-sixth pass — delegated a fresh research sweep to a subagent (given the full 85-pass exhausted-areas list to steer clear of). It found a shop-economy bypass one hop upstream of two already-closed gaps: the 2026-08-13 `equipItem()` fix (src/db.js) gates equipping a paid cosmetic on `itemKey` appearing in the `shop_owned` settings value, and the 2026-08-14 `importGameData()` fix validates restored `equip_*` slots against `shop_owned` — but neither pass touched `settings:set`'s guard list (main.js), which already special-cases `equip_*` keys (rejected, per the 08-13 fix) and `day_rollover_hour` (bounds-checked) but left `shop_owned` itself as a plain pass-through to `db.setSetting()`. Since `shop_owned` is only ever supposed to grow via `purchaseItem()`'s coin-deducting transaction or the validated import path, anyone driving the IPC bridge directly (`window.sychboard.settings.set('shop_owned', JSON.stringify(['bg_aurora','accent_gold','card_glass','orb_gold']))`, same devtools threat model as every other shop-trust fix) can forge ownership of any paid cosmetic with `sychcoins` untouched, then simply click "Equip" in the ordinary Shop UI — the legitimate `shop:equip` → `equipItem()` path reads the forged `shop_owned` list, finds the item "owned," and equips it for free, a complete re-opening of the SychCoins economy bypass the 08-13/08-14 passes specifically closed elsewhere. Fixed by adding `shop_owned` to `settings:set`'s rejection list in main.js, mirroring the existing `equip_*` guard; confirmed via grep the only renderer read of `shop_owned` is a `settings.get` (never a `.set`), so no legitimate call site is affected. `node --check` passes on all four core files.
- 2026-08-16: eighty-seventh pass (second today) — delegated a fresh research sweep to a subagent (given the full 86-pass exhausted-areas list to steer clear of). It found the single highest-severity gap uncovered since this routine started: `sanitizeText()` itself (`src/renderer.js:141-146`) — the one shared helper the entire ~10-pass, ~20-site stored-XSS sweep (2026-07-26 through 2026-08-08: habits/goals/todos/notes/events/shifts/exam-date/username/dev-todos/holiday-fields/YouTube-titles/boot-chat, plus `sanitizeImportedValue()`'s backup-restore hardening) relied on to be safe — escaped `<`/`>`/`"`/`'` but never `&`, unlike its siblings `parseMD()`/`escAttr()` (both correctly escape `&` first). Since it never touches `&`, feeding it a pre-encoded payload like `&lt;img src=x onerror=alert(1)&gt;` (a literal ampersand-l-t-semicolon string, no raw angle brackets at all) passes straight through unchanged; when that stored string is later interpolated into `innerHTML`, the browser's HTML parser decodes the entities back into a live `<img onerror=...>` tag and executes it — a complete bypass of every one of the ~20 fixed call sites at once, reachable through any ordinary text field (e.g. Habits → Add habit → `&lt;img src=x onerror=alert(1)&gt;`) or a crafted backup file, in this privileged Electron renderer with access to API keys and the git-write MCP tools. Verified directly by reading the function against `parseMD()`/`escAttr()`'s correct pattern, then reproducing standalone: the old regex left `&lt;img...&gt;` byte-for-byte intact; fixed by escaping `&` first (`s.replace(/&/g,'&amp;').replace(/[<>\"']/g,...)`, matching the sibling helpers exactly) — the same payload now round-trips as fully inert `&amp;lt;img...&amp;gt;` (decodes once to literal text, not markup), while ordinary legitimate input (`Tom & Jerry`, `O'Brien`, a genuine `<script>` attempt) still escapes exactly as before. One-line fix in the shared helper closes every affected site at once; no other call site does its own escaping. `node --check` passes on all four core files. (Backup finding not pursued this pass: a minor freeze/milestone-token accounting drift in `uncompleteQuest` for same-day multi-quest categories — lower severity, deferred to a future pass.)
- 2026-08-16: eighty-eighth pass (third today) — picked up the previous pass's deferred finding: a freeze/milestone-token accounting drift in `uncompleteQuest` (src/db.js) for multi-quest categories. `freeze_used`/`milestone_token_granted` are day-level streak-transition effects completeQuest only ever sets on the FIRST completion of a day for a category (a later same-day completion in the same category hits the `lastDate===appDate` no-op branch and never re-derives them), but `uncompleteQuest`'s clawback only reads those flags off the *specific* completion row being deleted, and only acts at all when it's the last completion left for that date (`otherOnDate===0`). In a category with 2+ daily quests: complete quest A (hits a 7-day milestone, flag lands on A's row, +1 freeze token), complete quest B (same day, no flags) — then uncomplete A first: `otherOnDate>0` (B remains) correctly no-ops the streak revert (the day still counts), but A's row carrying the milestone flag is deleted outright, permanently orphaning it; when B is later uncompleted too (now the last of the day), it has no flags of its own, so the streak correctly reverts to 6 but the milestone token is never clawed back — net one leaked freeze token per cycle, capped only by the existing 3-per-category ceiling, the same farm shape the 2026-08-13 fix closed for the single-quest case. Fixed by migrating `freeze_used`/`milestone_token_granted` onto a same-day sibling completion (same category) before deleting a flagged row that still has one, so whichever completion ends up being the last deleted for that date is the one carrying the flags and correctly triggers the clawback, regardless of deletion order. Verified against a real SQLite DB (`node:sqlite` shim): a 2-quest category reproduction of the exact leak above now correctly ends at `{streak:6, tokens:0}` instead of leaking a token; the reverse deletion order (unflagged quest first, flagged one last) also correctly converges to `{streak:6, tokens:0}`; a single-quest-category control (the original 2026-08-13 fix's own test shape) is unaffected. `node --check` passes on all four core files.
- 2026-08-16: eighty-ninth pass (fourth today) — delegated a fresh research sweep to a subagent (given the full 88-pass exhausted-areas list to steer clear of). It found one more instance of the "AI action-tag path skips a bound its sibling manual form enforces" class (already fixed six times before across `add_trip`/`update_yt`/`add_shift`/`set_balance`/this same tag's own `day` field) at a field none of those touched: the AI's `[ADD_SUBSCRIPTION:name:amount:day]` tag (`parseActions()`, src/renderer.js:1509) parsed `amount` as `parseFloat(amount)||0` with only a `>0` floor at the write site, no ceiling — while the manual "Add Bill" form (`addSub()`, line 1946: `validateNumber(...,0.01,99999)`) caps the identical field at 99999. A plausible AI reply like `[ADD_SUBSCRIPTION:Rent:50000000:1]` (a misheard/garbled figure) stored the amount verbatim, rendering uncapped in the Bills list and feeding a corrupted figure back into the AI's own system-prompt finance context on later turns. Fixed by clamping at the parse site: `Math.min(Math.max(parseFloat(amount)||0,0),99999)`, matching `addSub()`'s own range. `node --check` passes on all four core files.
- 2026-08-17: ninetieth pass — delegated a fresh research sweep to a subagent (given the full 89-pass exhausted-areas list to steer clear of). It found a pre-existing copy-paste bug in `rYT()` (src/renderer.js:1974), the YouTube tab's stat renderer, present unchanged since the very first commit that created renderer.js (confirmed via `git log -p -S`) and never mentioned anywhere in this log. The "Videos" metric card rendered `${st.yt.videoCount||satDays()+'d'}` — falling back to `satDays()` (a real, correctly-used-elsewhere helper for "days until Saturday stream" countdowns, e.g. the boot chat message and the AI system-prompt's own "Days until Saturday stream" line) whenever `videoCount` is falsy. Since `videoCount` defaults to `0` and is only ever populated by a live YouTube Data API fetch, any fresh install or any user without a configured YouTube API key/Channel ID — ordinary, common usage, not an edge case — permanently saw the Videos stat display a day-count string like `"5d"` instead of `0`, silently changing value every day of the week with zero connection to actual video count. Fixed by replacing the erroneous fallback with a plain `0`, matching how the sibling `subs`/`views`/`hours` stats in the same block render (a direct value, no unrelated fallback). Verified `st.yt.videoCount` is always coerced to a number in `load()` (`if(st.yt.videoCount==null)st.yt.videoCount=0`) so `||0` is a safe no-op guard, and confirmed via grep that the other two `satDays()` call sites are legitimate uses, so no other site needed touching. `node --check` passes on all four core files.
- 2026-08-17: ninety-first pass (second today) — delegated a fresh research sweep to a subagent (given the full 90-pass exhausted-areas list to steer clear of). It found one more live instance of the app-date-vs-real-date mixing class in `satDays()` itself (src/renderer.js:174), the "days until Saturday stream" countdown helper this morning's own pass (90, above) had just touched — but only for its *callers'* fallback behavior, not its own internal weekday derivation, which still used raw `new Date().getDay()` (real wall-clock weekday) instead of the rollover-hour-aware `appWeekdayIndex()` helper every sibling "today" concept (Schedule, Home Upcoming, the AI system-prompt's own "Today (Weekday, ...)" line one row above `satDays()`'s own call site in `callGroq()`) was migrated to across ~8 dedicated passes on 2026-08-10 through 08-12. Confirmed via grep that `satDays`'s own definition was never examined for this bug before, only its call sites. Concrete failure: between local midnight and the rollover hour (default 4am), the rest of the dashboard already treats it as the previous app-date, but `satDays()` still answered from the real wall-clock weekday — e.g. at Saturday 01:00 with the app-date still Friday, the old code reported "stream is today" (`7`) when the app-date-consistent answer is "tomorrow" (`1`), directly contradicting the very next line of the AI's own system prompt (`Today (Friday, ...)`) and producing a self-contradictory boot-screen greeting. Fixed by deriving from `appWeekdayIndex()` (Mon=0..Sun=6) instead: `const diff=(5-aw+7)%7;return diff||7;`. Verified with a standalone Node reproduction (deleted after use) comparing old vs. new across the midnight-to-rollover boundary and a full week of post-rollover times: the boundary case above now correctly returns `1` instead of `7`; a second boundary case (Sun 02:00, app-date=Saturday) now correctly returns `7` instead of `6`; every time-of-day after the rollover hour across a full week produces identical old-vs-new output, confirming no regression. `appWeekdayIndex()` is a hoisted `function` declaration, so calling it from `satDays()` (defined earlier in the file) is safe, matching the existing pattern (e.g. `callGroq()` already calls it before its own later declaration). `node --check` passes on all four core files.
- 2026-08-17: ninety-second pass (third today) — delegated a fresh research sweep to a subagent (given the full 91-pass exhausted-areas list to steer clear of). It found one more unaudited instance of the falsy-zero-default class (already fixed at `updateDev()`, `updateYT()`, `addShift()`'s wage field, and the Settings default-wage button): `savePomSettings()` (src/renderer.js) parsed the Pomodoro tab's Focus/Break minute fields as `parseInt(...)||25`/`||5` — since neither `#pom-f-in` nor `#pom-b-in` (src/index.html) has a `min` attribute, a user can type `0`, and `parseInt('0')` is falsy, so an explicit `0` silently got replaced with the hardcoded default before `validateNumber()` ever had a chance to clamp it to its real minimum of `1`, with the "Timer settings saved" toast still firing as if the typed value was honored. The Pomodoro timer was touched by exactly one prior pass (2026-08-04, the `togglePom()` stuck-at-00:00 fix) but `savePomSettings()` itself was never checked for this pattern. Fixed by reading the raw trimmed field value and only falling back to the default when it's genuinely blank, otherwise passing the parsed value (including `0`) into `validateNumber()` so it's clamped to `1` instead of silently overridden — same shape as the `updateDev()`/`updateYT()` fixes. `node --check` passes on all four core files.
- 2026-08-17: ninety-third pass (fourth today) — a first research sweep this pass proposed a `shop_owned`-forgery finding in `importGameData()` (src/db.js); on investigation this was a dead end (write-up below) and was reverted before a second sweep found the real fix landed instead. **Dead end, reverted:** filtering restored `shop_owned` to known `SHOP_CATALOG` keys does nothing observable — garbage keys in `shop_owned` were already inert (the renderer only ever renders Buy/Equip buttons by iterating its own fixed `SHOP_ITEMS` catalog, never `owned`, and `equipItem()`/the existing equip_* revalidation already independently check `SHOP_CATALOG[itemKey]` exists) — and more fundamentally, `importGameData()` already writes `profile.sychcoins` from the imported file with only a floor, no ceiling, so a crafted backup can already set coins to any value and legitimately buy everything through the real, already-hardened `purchaseItem()` — no `shop_owned` forgery needed, and since every item totals under 1000 coins, a numeric ceiling on coins wouldn't close that path either. This whole family (a user hand-editing their own local single-player backup file before restoring it — the "attacker" and the "victim" are the same person) is architecturally different from this routine's previously-fixed IPC/devtools-reachable gaps and has no clean minimal fix without a real purchase ledger the app doesn't have; correctly a design decision for Daniel, not a one-line patch, so left alone rather than ship a fix verified to change nothing.

  **Real bug found and fixed instead:** `uncompleteQuest()` (src/db.js) corrupts `streaks.current_streak` when reverting a completion that landed on `completeQuest()`'s broken-streak branch. That branch sets `current_streak` to a soft-reset value (`floor(oldStreak*0.5)+1`, the default) or hard-reset `1` — a non-linear transform of the pre-completion streak — but `uncompleteQuest`'s revert always did `current_streak=MAX(0,current_streak-1)`, an inverse that's only correct for the plain +1 and freeze-bridge branches. Concretely: a category with a genuinely broken 20-day streak, completed today (soft-resets to 11 via the default setting), then unticked (an ordinary mis-tap correction) — old code left `current_streak=MAX(0,11-1)=10`, not the true `20`, with zero user-visible indication, and since `current_streak` is what the *next* soft-reset reads as its baseline, the corruption compounds indefinitely. Reachable through completely normal complete/uncomplete toggling, no forged input needed. Fixed by adding migration v7 (`quest_completions.pre_streak`), recording the true pre-completion streak value at the moment `completeQuest` actually updates the `streaks` row (both the existing-streak and first-ever-completion branches), and having `uncompleteQuest` restore `current_streak` directly from that recorded value when present, instead of assuming `-1` — with the sibling-migration mechanism already used for `freeze_used`/`milestone_token_granted` (2026-08-16) extended to carry `pre_streak` too, so a multi-quest category still restores correctly regardless of which same-day completion is deleted last. Rows predating the migration (or an older imported backup) fall back to the old `-1` approximation rather than fail. Also carried `pre_streak` through `exportGameData`/`importGameData` for backup round-trip fidelity, matching the existing convention for the sibling flags. Verified against a real SQLite DB (`node:sqlite` shim): the 20→(soft-reset 11)→uncomplete reproduction above now correctly restores `20` (was `10`); a control plain +1/-1 case (streak 3→4→3) is unaffected; the 2-quest-same-day sibling-migration case (complete both, uncomplete the streak-changing one first, then the other last) also correctly restores the true pre-day value via the migrated `pre_streak`; `initDB()` double-invocation confirms migration v7 is idempotent (27 quests seeded, no duplicate-column error). `node --check` passes on all four core files.
- 2026-08-18: ninety-fourth pass — delegated a fresh research sweep to a subagent (given the full 93-pass exhausted-areas list to steer clear of). It found a real bug in `rShifts()` (src/renderer.js), the Finance tab's "This month" shift-earnings filter: the shift `date` field is free text with no year (placeholder `"18 Apr"`), so the filter always appended the current year before parsing (`new Date(s.date+' '+now.getFullYear())`) — but a shift added via the AI assistant's `[ADD_SHIFT:date:hours:wage]` tag (no format hint given, LLMs default to full/ISO dates) or typed by a user with an explicit year already carries one, so the blind append produces an unparseable string (`"2026-08-14 2026"` → `Invalid Date`), and `Date` comparisons against `Invalid Date` are always false — the shift silently drops out of the "This month" sum only (still correct in the list and "All time" total), quietly undercounting the month's earnings with no error anywhere. Fixed by adding `parseShiftDate(s,year)`, which only appends the fallback year when the string doesn't already contain a 4-digit year. Verified with a standalone Node reproduction: the placeholder day+month format is unaffected; the previously-broken full-date case (`"2026-08-14"`) now parses and counts correctly, where the old code silently excluded it. `node --check` passes on all four core files.
- 2026-08-18: ninety-fifth pass (second today) — delegated a fresh research sweep to a subagent (given the full 94-pass exhausted-areas list to steer clear of). It found a real bug in `obFinish()` (src/renderer.js), the function that builds `st.sections` at the end of normal onboarding: its own `sectionMap`/`always` list never had a `sleep` entry, unlike both `CORE_SECTIONS` (the pre-onboarding default) and `activateDanielMode()` (the hidden Easter egg), which had already drifted to include it. Since the sidebar, mobile nav, Home grid, and Manage Sections all render purely by iterating the saved `st.sections` array (only `goPage()` itself has no such gate, so `[NAVIGATE:sleep]`/a direct call still worked), every user completing the real onboarding flow — essentially all real users, since Danielmode is a hidden username Easter egg — ended up with a fully working Sleep tracker (logging, 7-day chart, quality/consistency stats, targets, notifications) with no link anywhere in the UI to reach it, and no way to re-add it ("Add Section" only creates new custom sections). Fixed by adding `sleep` to both `sectionMap` and `always` in `obFinish()`, plus a one-time backward-compatible migration in `load()` that pushes a `sleep` entry into `st.sections` for any already-onboarded user missing one (without it, the code fix alone would only help brand-new installs, since `obFinish()` only ever runs once per install). Verified with a standalone Node reproduction of the exact post-fix logic: `sleep` is present in `st.sections` for onboarding with no/partial/all focus areas selected; the `load()`-time migration adds it for an already-onboarded user missing it, is idempotent on repeated `load()` calls, and correctly leaves a not-yet-onboarded user's sections untouched. `node --check` passes on all four core files.
- 2026-08-18: ninety-sixth pass (third today) — delegated a fresh research sweep to a subagent (given the full 95-pass exhausted-areas list to steer clear of). It found the AI action-tag `add_todo`/`add_habit`/`add_goal` branches in `executeActions()` (src/renderer.js, ~lines 1539-1541) never got the whitespace-only-capture guard the 2026-08-15 pass (line 298 above) added to the five *match/destructive* action tags (`complete_habit`/`complete_goal`/`delete_todo`/`remove_habit`/`remove_event`) for the same `([^\]]+)` regex degenerate case — `[ADD_TODO: ]`/`[ADD_HABIT: ]`/`[ADD_GOAL: ]` (a plausible malformed-LLM-reply shape, not a contrived attack, same as the sibling fix's own justification) push a blank, perpetually-unchecked/undone row into `st.genTodos`/`st.habits`/`st.goals` with no truthiness check on `a.val`. Concretely for habits: `rHabits()`'s "all habits done" confetti trigger (`st.habits.length>0&&st.habits.every(h=>h.done)`) can never fire again once a blank habit is silently added, since it renders with no label text (only its delete button) and nothing prompts the user to notice and remove it — same failure shape reaches Todos/Goals too. Fixed by adding `&&a.val` to all three branches, matching the exact guard style already used on the five sibling branches. Verified with a standalone Node reproduction of the parse+execute logic: `[ADD_TODO: ] [ADD_HABIT: ] [ADD_GOAL: ]` now correctly no-ops (`changed:false`, all three lists empty) where it previously pushed three blank rows; real tags (`[ADD_TODO:Buy milk] [ADD_HABIT:Read] [ADD_GOAL:Save 100|finance]`) are unaffected. `node --check` passes on all four core files.
- 2026-08-18: ninety-seventh pass (fourth today) — delegated a fresh research sweep to a subagent (given the full 96-pass exhausted-areas list to steer clear of). It found a real notification-suppression bug in `sendAINudge()` (src/renderer.js) — the "AI Nudge" proactive-observation notification stamped `st.notifLastSent.aiNudge=today` and persisted it *before* awaiting `callGroq()`, rather than after a successful send, unlike its sibling reminders (`bedReminder`/`morningBrief`/`habitReminder`), which only stamp their flag after their own effectively-infallible `sendNotif()` call. `callGroq()` returns `null` on any failure (network error, invalid/expired/revoked Groq key, malformed API response — all normalized by its own try/catch), and the actual `sendNotif()` call only runs inside the `if(r){...}` success branch, but by then the "already sent today" flag was already written regardless of outcome. Concrete failure: a single transient failure at the scheduled nudge time (a network hiccup, a since-revoked key, Groq rate-limiting) silently disables every retry for the rest of the day, since `checkNotifications()`'s 60s poll checks `nl.aiNudge!==today` and finds it already stamped — indistinguishable from "working as configured," and if the underlying cause persists (e.g. a dead key), the feature silently disables itself one day at a time forever. Fixed by moving the stamp+`save()` inside the `if(r){...}` success branch, so it's only recorded once a nudge is actually delivered. Verified with a standalone reproduction contrasting old vs. new logic against a failing and a succeeding `callGroq()` result: old code stamps `aiNudge` as sent even on failure (blocking retries that day); new code leaves it unset on failure (next poll retries) and still correctly stamps once on genuine success. Also investigated and declined two other leads as not solid enough: the now-cosmetically-inert Boot Orb shop category (`window._orbTheme` has no consumer since the 2026-07-25 boot-screen strip, but that redesign is explicitly Daniel's own WIP and out of scope) and `completeQuest()`'s `streakBonusPct` being derived from the pre- rather than post-completion streak (on reflection matches what's actually shown on screen before the action, so self-consistent design, not a bug). `node --check` passes on all four core files.
- 2026-08-19: ninety-eighth pass — delegated a fresh research sweep to a subagent (given the full 97-pass exhausted-areas list to steer clear of; also checked preload.js's bridge surface against every main.js IPC handler and every `JSON.parse` call site — both clean). It found the AI action-tag `add_trip`/`add_event` branches in `executeActions()` (src/renderer.js) never got the whitespace-only-capture guard the 2026-08-15/08-18 passes added to eight sibling `add_*`/match-type action tags for the same `[^:]+`/`([^\]]*)` regex degenerate case, and unlike their own manual forms (`addTrip()` requires a non-empty destination; `addSchedEvent()` requires non-empty text) — `[ADD_TRIP:   :2026-09-01:500]`/`[ADD_EVENT:Mon:   :]` (whitespace-only field) silently pushed a blank-destination trip or a blank schedule chip with no rejection. Fixed by adding `&&a.dest`/`&&a.name` to the two branches, matching the exact guard style already used elsewhere (confirmed those are the correct parsed field names via the `ADD_EVENT`/`ADD_TRIP` regex definitions). Verified with a standalone Node reproduction of the parse+execute logic: whitespace-only trip/event tags now correctly no-op where they previously pushed blank entries; real tags are unaffected. `node --check` passes on all four core files.
